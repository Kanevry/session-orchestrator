# Mutation-Testing Report — 8-Module Sample (GL #910, W3)

**Date:** 2026-07-31
**Branch:** `feat/938-panel-follow-ups-tv003`
**Method:** Hand-mutants applied to a transient in-place copy of each module
(backup → exact-string replace → run the module's existing test subset →
**always restore from backup**). The repo module source is UNCHANGED after each
run (verified via `git status`: none of the 8 target modules appear in the diff).
Driver: `scratchpad/run-mutants.mjs`. A mutant is **KILLED** if the existing
suite goes red, **SURVIVOR** if it stays green.

## Headline

| # | Module | Mutants | Killed | Survivors | Kill-rate |
|---|--------|--------:|-------:|----------:|----------:|
| 1 | `scripts/lib/mode-selector.mjs` | 6 | 5 | 1 | 83% |
| 2 | `scripts/lib/wave-resource-gate.mjs` | 8 | 4 | 4 | 50% |
| 3 | `scripts/lib/telemetry/consent.mjs` | 7 | 7 | 0 | **100%** |
| 4 | `scripts/lib/session-registry.mjs` | 7 | 2 | 5 | 29% |
| 5 | `scripts/lib/scope-baseline.mjs` | 5 | 3 | 2 | 60% |
| 6 | `scripts/lib/reconcile/engine.mjs` | 4 | 2 | 2 | 50% |
| 7 | `scripts/lib/memory-proposals/store.mjs` | 5 | 5 | 0 | **100%** |
| 8 | `scripts/lib/vault-status/board-writer.mjs` | 4 | 4 | 0 | **100%** |
| — | **Total** | **46** | **32** | **14** | **70%** |

Prior round (3 modules, 24 mutants) killed 75%. This round's 70% over a wider,
deliberately boundary-heavy sample.

## The survivor pattern (confirms + extends the prior finding)

The prior session found: *leaf functions tested thoroughly, wiring/config-precedence
not at all.* This round's 14 survivors split into **two** clean patterns:

**Pattern A — untested threshold/boundary comparisons (9 survivors).** Every
`<`/`<=`, `>`/`>=`, `>=1`/`>1` flip that only matters when the value sits
*exactly* on the limit survived, because the suites exercise thresholds strictly
above/below, never AT the boundary: `wr4 wr5 wr6 wr7` (wave-resource-gate),
`sr1 sr2 sr7` (session-registry), `re1` (reconcile cap), `ms5` (mode-selector filter).

**Pattern B — untested wiring / validation / fail-safe (5 survivors).** The
same class the prior round named, still under-covered: `sr4` (schema-v2 mode
type-check inversion), `sr6` (fail-open NaN-age), `sb2` (div-by-zero guard),
`re4` (confidence default), `sb4` (near-equivalent field echo).

The two 100%-kill modules (`consent`, `store`) and near-100% (`board-writer`)
are the counter-evidence: where a suite *does* enter the boundary and the
negative/error branch (consent's fail-closed precedence, store's quota/floor
boundaries, board-writer's status-sticky guards), mutants die. **A module with
100% kill-rate is a success — it needs no new tests.**

## Need-gated decisions

### Tests WRITTEN (2 new files, 8 tests, kill 7 survivors)

`tests/unit/mutation-wave-resource-gate.test.mjs` (4 tests) — kills `wr4 wr5 wr6 wr7`:
- **wr5** — RAM free EXACTLY at critical must NOT escalate to coordinator-direct.
- **wr6** — CPU EXACTLY at max must NOT halve the wave.
- **wr7** — concurrent EXACTLY at warn must NOT attach a warn reason.
- **wr4** — a heavy-repo wave whose planned agents *equal* the cap must not be
  spuriously downgraded to `reduce` with a bogus cap reason.

`tests/unit/mutation-session-registry.test.mjs` (4 tests) — kills `sr4 sr6 sr1`:
- **sr4** (2 tests) — a valid string `mode` entry stays fresh; a non-string
  `mode` is rejected. The inverted mutant dropped valid modes AND accepted
  malformed ones — a real schema-v2 validation bug no test entered.
- **sr6** — an entry with an unparseable `last_heartbeat` is NOT fresh
  (fail-safe). The mutant `NaN→0` would make a corrupt timestamp a phantom live
  peer that never gets swept.
- **sr1** — an entry aged EXACTLY at the freshness window is still fresh.

Both files placed under `tests/unit/mutation-*.test.mjs` (new dedicated files)
to avoid touching sibling-owned test files (`consent.mjs`/`store.mjs` tests live
outside this task's scope). Fake-regression verified: each survivor mutation,
re-applied against the NEW test file, turns it RED (KILLED); reverted → green.

### Deliberate NON-investment (degenerate / near-equivalent)

- **ms5** (mode-selector `filter >= 0.1 → > 0.1`) — only observable when a
  global-max-swap alternative has confidence *exactly* 0.1; alternatives are
  surfaced suggestions, so dropping one at the 0.1 cutoff has negligible
  consequence. Not a nameable production bug.
- **sb4** (scope-baseline planned-fallback `0 → 1`) — **near-equivalent**: both
  0 and 1 resolve to denominator `Math.max(_, 1) = 1`, so `filesRatio` is
  identical; only the echoed `plannedFiles` field differs. No behavioural bug.

### Nameable but deferred (setup cost > W3 budget) → OPEN-QUESTIONS

- **re1** (reconcile `maxProposalsPerRun >= 1 → > 1`) — setting the volume brake
  to *exactly 1* is silently replaced by the default of 10; the tightest brake
  setting is ignored. Same class as the panel-found "never-passed-through
  max-proposals" HIGH bug. Needs a `runReconcile({ dryRun, learnings, maxProposalsPerRun: 1 })`
  fixture; deferred for budget, repro documented.
- **sb2** (scope-baseline `Math.max(plannedFiles, 1) → , 0`) — a baseline with
  `plannedFiles === 0` yields an Infinity/NaN ratio instead of a finite one.
  Needs a full git-repo + STATE.md frontmatter fixture (the existing
  scope-baseline harness cost); deferred.
- **sr2** (sweep age-boundary), **sr7** (detectPeers age-boundary) — the same
  `<=`/`>` boundary semantics as sr1, but reached only through async fs registry
  setup. sr7 duplicates the `isRegistryEntryFresh` rule now pinned by sr1;
  sr2 (sweep threshold) is a lower-value boundary follow-up.
- **re4** (reconcile confidence-default `0 → 1`) — a learning with non-numeric
  confidence sorts to the top instead of the bottom of the volume-brake cut;
  narrow, low blast-radius. Follow-up.

## Verification

- New tests green: `tests/unit/mutation-wave-resource-gate.test.mjs` +
  `tests/unit/mutation-session-registry.test.mjs` → 8 passed.
- Final suite run (21 affected test files incl. new): **510 passed / 0 failed**.
- Fake-regression: all 7 targeted survivor mutations KILLED by the new tests.
- Module sources unchanged (git status: none of the 8 targets in the diff).
