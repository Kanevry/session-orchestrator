# Sunset Review — 2026-07-31

> Read-only advisory report per `skills/sunset-review/SKILL.md`. **Zero deletions, zero auto-filed issues.** Every candidate below is a decision for the operator, not an action taken.
> Filed against GitLab issue #866 (cold skill/flag-tail review). Companion measurement: `.orchestrator/metrics/context-overhead-866.json`.

## Run parameters

- Walker: `node scripts/lib/sunset/walker.mjs --json --window-days 90`
- Generated at: `2026-07-31T06:15:47.885Z`
- Window: 90 days
- Telemetry coverage: **78.06 days** (`subagents.jsonl` earliest 2026-05-14, `skill-invocations.jsonl` earliest 2026-06-16)
- **`meta.lowConfidence: true`** — coverage (78d) < window (90d). Per the walker's mandatory guardrail, **every `Retire` verdict was downgraded to `Investigate`**. Nothing is safe to retire this run; treat all cold findings as "investigate", not "delete".

## Verdict summary

| Verdict | Count |
|---|---|
| Retire | **0** (all downgraded → Investigate under lowConfidence) |
| Demote | 0 |
| Investigate | 17 |
| Active | 69 |
| **Total surface items** | **86** |

## Investigate candidates (17)

Grouped by kind. `disp` = dispatch/invocation count in window; `nbr` = non-boilerplate refs. All carry `lowConfidence: true`.

### Skills (7)

| Skill | disp | nbr | Signal / reason |
|---|---|---|---|
| `convergence-monitoring` | 0 | 1 | 1 cross-ref, no command linkage. Documented consumer: `/autoresearch` + wave-executor inter-wave checkpoints — likely load-bearing but rarely dispatched standalone. |
| `domain-model` | 0 | 0 | Cold (0 dispatch, 0 non-boilerplate refs) → downgraded. DDD-support skill; verify against active usage before any action. |
| `frontmatter-guard` | 0 | 1 | 1 cross-ref (wave-executor pre-dispatch hook), no command linkage. Fires mechanically on vault-write scope — dispatch telemetry undercounts hook-driven use. |
| `mcp-builder` | 0 | 0 | Cold. On-demand authoring skill (build an MCP server) — zero-use in-window is expected for a rarely-needed capability, not necessarily dead. |
| `npm-publish` | 0 | 0 | Cold in dispatch telemetry, but the memory log records a 2026-07-19 npm first-publish that used it. Telemetry blind spot (coordinator-direct skill use), NOT truly cold. |
| `skill-creator` | 0 | 0 | Cold. Meta-authoring skill; used interactively, undercounted by dispatch telemetry. |
| `ubiquitous-language` | 0 | 0 | Cold (0/0). DDD-vocabulary support skill; verify against `architecture`/`domain-model` usage. |

### Agents (6)

| Agent | disp | nbr | Signal / reason |
|---|---|---|---|
| `db-specialist` | 1 | 4 | Near-zero (last 2026-05-19). Real cross-refs (2 strict). Demote candidate on cleaner telemetry — kept load-bearing for DB-heavy waves. |
| `eval-judge` | 0 | 3 | Conflicting signals (0 dispatch but 2 strict + 4 prose refs). Consumed by `/eval --judge` opt-in path (default off) → dispatch-cold by design. |
| `memory-proposal-collector` | 0 | 1 | Conflicting (0 dispatch, 2 prose refs). By-design reference doc (per SKILL.md example) — never dispatched, not a defect. |
| `skill-applied-judge` | 0 | 2 | Conflicting (2 strict + 3 prose refs). Opt-in skill-evolution judge path (default off). |
| `ui-developer` | 1 | 3 | Near-zero (last 2026-05-19). Frontend-only agent; cold because the repo is Node/Markdown, no UI surface. |
| `ux-evaluator` | 1 | 6 | Near-zero (last 2026-05-14) but 6 non-boilerplate refs. Consumed by `test-runner` on web/macOS targets — cold because no E2E runs in-window. |

### Commands (4)

| Command | nbr | Signal / reason |
|---|---|---|
| `autopilot-multi` | 1 | Invokes no skill, 1 cross-ref. Multi-story autopilot — low-use by design (heavy operation). |
| `contract-version-bump` | 0 | Cold (0/0). On-demand contract-bump helper. |
| `spinout` | 1 | 1 cross-ref. Venture-spinout runbook — rare-by-design (used from parent Ventures context, not this repo). |
| `templates-ack` | 1 | 1 cross-ref. Per-session policy-ack bypass — invoked ad hoc, undercounted. |

## Recommendations (advisory — operator decides)

1. **Do NOT retire anything this run.** `lowConfidence: true` (78d < 90d). Re-run after ≥90 days of telemetry accrues (~2026-08-12) for a first trustworthy Retire signal.
2. **Telemetry blind spots dominate the cold list.** At least `npm-publish` (used 2026-07-19), `skill-creator`, and `mcp-builder` are coordinator-direct / on-demand skills that dispatch telemetry structurally undercounts — their coldness is a measurement gap, not disuse. Opt-in-gated agents (`eval-judge`, `skill-applied-judge`, `memory-proposal-collector`) are cold by design (default-off features). None are prune candidates on this data.
3. **Genuinely capability-absent items** — `ui-developer`, `ux-evaluator`, `domain-model`, `ubiquitous-language` — are cold because this repo has no UI/E2E/DDD surface, not because they are broken. Keep for portability to consumer repos that do.
4. **Next-quarter watch:** if `contract-version-bump` and `autopilot-multi` stay at 0 dispatch through the next full-window run, they are the strongest first Demote candidates.

## The #866 flag-tail premise (opt-in flags off-by-default)

#866 also cited "~25 opt-in flags off-by-default" as sunset surface. This is **by design, not dead weight**: off-by-default is the fail-closed posture the repo mandates (`fail-open-config-gate-antipattern` learning). An unused opt-in flag costs ~0 runtime — it is parsed only when its block is present and enabled. Sunsetting a flag means removing a capability, which is an operator product decision, not a usage-telemetry verdict. No flag is recommended for removal on usage grounds.

## Cadence

Last-run recorded for the quarterly nudge. Next review due ~2026-10-31 (or after the telemetry window first reaches 90 days, whichever surfaces first).
