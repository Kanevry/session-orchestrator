# d13 — projects-baseline dependency — 2026-09-06 @ e4674109
## Summary
- Census 49 files: 17 docs-only · 15 test-only · 13 soft · 4 hard-runtime · of which 2 DEAD (hardcoded default $HOME/Projects/projects-baseline does not exist on this host; clones live at intern/ and Bernhard/).
- LIVE DEFECT: scripts/lib/frontmatter-guard.mjs:17-21 SCHEMA_SOURCE_PATH hardcoded → readVaultSchema() → null → generateFrontmatterSnippet() THROWS ("Cannot destructure property 'typeEnum' of 'schema' as it is undefined"); computeSchemaHash() returns e3b0c442 (SHA-256 of "") silently. Hits THIS host now, not only external users.
- scripts/lib/vault-backfill/template.mjs:13-15,30: default path missing → dieFn(2) hard abort unless PROJECTS_BASELINE_DIR. scripts/sync-vault-schema.mjs --check exit 1 (validator.mjs has 'peer-card'/'board' in type enum, sibling clone doesn't; CI gate needs SCHEMA_DRIFT_TOKEN). skills/plan/SKILL.md: `/plan new` hard-stops without plan-baseline-path; feature/retro unaffected.
- External user: public-fallback.md decides silently → PATH_TYPE=public; lock source claude-init/plugin-template; plugin templates (5 archetypes) used; rules fetch (Phase 3.5) never fires without baseline-ref + GITLAB_TOKEN.
- Templates plugin vs baseline: 0 archetype overlap (plugin 42 files/5 archetypes; baseline 18 archetypes, shared alone 122 files) → not "drift", two separate corpora. #1100's "3.5.0" marker does not exist in the plugin; baseline lock shows plugin-version 3.9.0 / refreshed 3.19.0 → #1100 premise stale.
- Rules: .claude/rules hand-written vs baseline: 0/6 identical; plugin ahead on parallel-sessions (281 vs 52), security, testing; baseline ahead on cli-design, development; 12 plugin-only, 24 baseline-only. SECOND drift axis: rules/always-on/ (the distributable library rules-sync.mjs ships) vs .claude/rules: 9 shared names all drifted, 2 only in rules/, and security/testing/development/mvp-scope/cli-design MISSING from rules/ entirely (#1096 FA2 gap).
- Company-stack share of 18 hand-written always-on files (2,278 lines): Tier A (externally unusable: @your-org, requireAuth, Supabase, service_role, PostgREST) = 24 lines (1.1 %) in security.md 13 / testing.md 10 / vbc 1; Tier B (opinionated but public) 59; total 4.6 %. Core corpus already generic.
- Fleet reads rules from the BASELINE, not the plugin: aiat-barrierefrei-engine 29 rule files, 24 match baseline names, 5 plugin library, 0 carry plugin provenance header → rules-sync.mjs never applied there. Any plugin-only change has no direct fleet risk.
- rules/opt-in-stack/ already exists (backend, backend-data, frontend, security-web, swift).
## Decoupling options
(a) optional private overlay — RECOMMENDED: 4 hard-runtime → soft with fallback + message, 2 dead paths → resolver; ~6 files; effort S–M; fleet risk low; consistent with #729 2026-07-10 decision (two masters). (b) upstream-lift: + migrate 24 baseline-only rules + build propagation channel to 25 repos (0 headers today); L; high fleet risk; blocked by #881. (c) fork: negative value.
## Minimal first step (p5)
1 frontmatter-guard.mjs: SCHEMA_SOURCE_PATH via resolveHostPath('baseline-path') (scripts/lib/config/host-paths.mjs); handle null schema (fallback to in-module regex set + WARN); computeSchemaHash null instead of empty hash. 2 vault-backfill/template.mjs same resolver + message mentions owner.yaml paths.baseline-path. 3 skills/frontmatter-guard/SKILL.md:100 + skills/daily/SKILL.md:140 replace dead literal path. 4 docs/baseline.md (~60 lines) linked from README + CLAUDE.md. Out of scope for p5: moving the 24 Tier-A lines to rules/opt-in-stack (separate diff, #1096 FA2).
Acceptance: validate-plugin exit 0; `node -e "import('./scripts/lib/frontmatter-guard.mjs').then(m=>m.generateFrontmatterSnippet())"` no throw (also with HOME=/tmp/x → WARN); vitest frontmatter-guard + config-baseline green; /tmp bootstrap without token → PATH_TYPE=public.
## Open questions
1 which clone is SSOT (owner.yaml → intern/, baselines: prefix rule → Bernhard/; vault-frontmatter.ts diverged 05-13 vs 09-02; CI gate checks which?). 2 rules/always-on vs .claude/rules SSOT (9/9 drifted). 3 should frontmatter-guard have an external schema source at all (vendor enum snapshot + sync gate like validator.mjs). 4 re-measure #1100 (AC says ≥3.21 vs title 3.5.0).
STATUS: done
