# d7 — Install-Journey — 2026-09-06 @ e4674109 (STATUS: partial — bootstrap prose AUQ count + fetch-baseline not read; d13 covers fetch-baseline)
## Summary
- Claude Code: works-with-friction. SessionStart hook in fresh consumer repo exit 0, clean systemMessage, stderr empty. 5 frictions: F1 hygiene banner blames the user for the 5 .orchestrator/ files the hook itself just wrote ("5 files neither tracked nor ignored") + "no CI pipeline" on a 1-commit repo; F2 SANDBOX LEAK: session-registry.mjs:48 hardcodes ~/.config/session-orchestrator/sessions (only SO_SESSION_REGISTRY_DIR override; ignores SO_CONFIG_HOME/resolvePrivateConfigDir) → a probe run registers in the operator's real registry (same class as the vault-dir gotcha); F3 first-run banner talks about 4 peers in OTHER repos; F4 DOC BREAK: docs/session-config-template.md:13 and :965 recommend `node scripts/parse-config.mjs --json` which does not exist (exit 1 "File not found: --json"); F5 not measured.
- Skipped-npm-install path: 4/27 hooks hard-crash (ERR_MODULE_NOT_FOUND js-yaml via owner-yaml.mjs): on-session-start, on-session-end, post-edit-validate, skill-invocation-telemetry; 23 degrade cleanly (destructive-guard prints loud "GUARD INACTIVE" banner, exit 0). GH#63 satisfied 23/27. picomatch is in NEITHER dependencies list but required lazily in rule-loader.mjs:79 and validate-vendored-rules.mjs:63 (GH#62 half-open?).
- Codex 0.144.4: works; all documented subcommands exist; `codex plugin marketplace upgrade` exists but codex-setup.md § Refresh says "re-run installer"; #1171 not verifiable (<0.149); #1226 documented in codex-setup.md only, README shows only clone path while codex-setup calls short-form "Recommended".
- Pi: `pi` not on the agent's non-interactive PATH (coordinator's shell has pi 2026.07.23) → #826 unverified; prompt parity 28/28 (count only).
- Cursor: real (hooks-cursor.json _enforcement: live, beforeShellExecution → destructive-guard bridge; cursor-install.mjs). origin/feat/cursor-native-adapter has 0 own commits (32 0) → delete branch.
- Copilot CLI: GH#54 fix in 93b40dd (v3.16.0, argument-hint quoted + YAML type guard in check-commands.mjs). But NO AGENTS.md at repo root (only CLAUDE.md) → a Copilot user gets no instruction file; no install path documented.
- UPGRADE GAP root cause (3 independent): (1) plugin-freshness probe runs `git log` in the plugin dir → marketplace cache is a file copy → "fatal: not a git repository" (inert for marketplace installs; and 28d < 30d threshold anyway); (2) classifyVersionMismatch in bootstrap-lock-freshness.mjs downgrades everything below a MAJOR jump to `info` ("minor drift only, no action required") — 5 minors = no action by design; (3) bootstrap.lock refreshed-plugin-version 3.24.0 was stamped from the CHECKOUT's package.json while 3.19.0 was loaded → in this repo $PLUGIN_ROOT resolves to the checkout so drift is invisible by construction. NO code compares installed vs available; no registry query anywhere except release.mjs. `claude plugin update <plugin>` exists ("restart required"). npm dist-tags.latest = 3.24.0 verified live.
- Smallest probe: scripts/lib/plugin-update-banner.mjs: installed = readPluginVersionFromPackageJson($CLAUDE_PLUGIN_ROOT) (already exported); latest = fetch registry.npmjs.org/session-orchestrator/latest, 2 s timeout, cache .orchestrator/runtime/plugin-latest.json 24h TTL; failure → null and SILENCE (never "up to date" from a failed fetch, #1031); minor or major behind → warn in the same systemMessage buffer with `/plugin update session-orchestrator` + restart; add plugin_version_installed/latest to session.started event (HR-105 falsifiability).
- ~/.config/session-orchestrator/sessions/sweep.log is 1.7 MB unrotated.
- Disclosure: agent removed exactly one self-created registry entry (9e17a5be…); no repo/harness writes.
## Recommendations
R1 lazy/try-catch js-yaml in the 4 hooks (M) — 27/27 exit 0 without node_modules. R2 plugin-update-banner + Phase-4 wiring + event fields (M). R3 parse-config --json alias or fix the 2 doc lines (S). R4 project-hygiene must not count its own .orchestrator artefacts (S). R5 session-registry via resolvePrivateConfigDir (S). R6 README Upgrade section + Codex short-form in install table (S). R7 delete origin/feat/cursor-native-adapter (S). R8 declare picomatch or document optional (S). R9 root AGENTS.md alias + instruction-delivery note for Copilot (M). R10 rotate sweep.log (S).
## Open questions
1 bootstrap prose AUQ count + fetch-baseline (→ d13). 2 does $PLUGIN_ROOT ever resolve to the cache in this repo? (decides which root R2 reads). 3 pi PATH. 4 picomatch deliberately optional? 5 can `codex plugin marketplace upgrade` replace the documented re-install refresh?
STATUS: partial
