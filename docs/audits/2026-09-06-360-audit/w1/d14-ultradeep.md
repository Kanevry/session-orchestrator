# d14 — ultradeep design — 2026-09-06 @ e4674109
## Summary
1 A real 4th enum value touches 48 files (12 code, 16 skills/commands/hooks, 10 docs, 10 tests); 9 hard closed sets; classifyMode() throws; telemetry/schema.mjs:83 silently degrades to 'other'; session-close-backfill.mjs:71 silently labels housekeeping.
2 RECOMMENDATION: NO new enum value. `/session ultradeep` = argument alias → PROFILE (session-type: deep + session-profile: ultradeep): ~8 touchpoints; telemetry via additive optional field session_profile (allowed by docs/telemetry.md § Schema evolution, no version bump; server has no enum, only MAX_SESSION_TYPE 32); external comprehension identical (argument-hint); low drift risk (no silent mislabel mode).
3 Only 2 things are new SHAPE: (a) a Research role with web tools (no role has WebSearch/WebFetch today; needs its own evidence rule: URL + retrieval date, PSA-006 analogue), (b) a Synthesis-Gate = wave with ZERO agents, coordinator-direct, blocking AUQ — session-plan Step 2 empty-role rule would DELETE it today; started-set verification has no expectation set for it. Everything else is existing keys: wave-reviewers (enabled:false here), max-turns, cross-repo, worker-pool (+background batches; NOT Workflow tool per LM-002b Stay verdict), custom-phases for release.
4 Finer: Quality → Review-Panel (read-only, always) + Test-Writing (write, earned); Discovery → Code-Discovery + Research; max-turns per ROLE not per session type (research 25–40, finalization 8).
5 Broader: deprecate `feature` as alias → deep with WARN (feature = 94/3,262 records = 2.9 %; 40 of 72 executed feature sessions ran the full 5 waves = deep with wrong label; complexity tier already sizes). Keep housekeeping unchanged (1,877/1,991 with 0 waves = express/stub lane, structurally different). 28 commands: not decidable from these data → sunset-review.
6 Core finding: 53 of 902 executed deep sessions (5.9 %) already ran >5 waves (46×6, 6×7, 1×8) — ultradeep exists, undeclared and unmeasured; naming it is the value.
## Profile spec (paste-ready PRD in agent output): 7 waves — W1 Research+Code-Discovery (≤18, read-only) → W2 Synthesis-Gate (0 agents, AUQ blocking, writes docs/audits/<date>-<slug>.md) → W3 Impl-Core ≤8 → W4 Impl-Polish ≤8 → W5 Review-Panel 3 read-only (RCR-007 triage) → W6 Quality min(cap, ceil((HIGH+MED)/3)) → W7 Release/Finalization ≤4. Defaults: agents-per-wave `ultradeep: 18` (override syntax already parses arbitrary keys — no code change), max-turns per role 40/25/15, wave-reviewers true [architect, qa, security], isolation worktree forced, Opus for W1/W2/W5/Impl-Core, Sonnet for Polish/Quality/Final. Mandatory artefacts: audit report, decision record (STATE.md deviations + ADR if architectural), CHANGELOG entry (missing → ultradeep_missing_audit event). Budgets as parity-exempt block `ultradeep: {max-agents-total 60, max-wall-clock-hours 6, max-output-tokens 12M, on-breach ask|stop}` checked at inter-wave checkpoints only (never mid-wave). Mode-selector: ≥2 signals of {scope ≥8 issues or tier complex, ≥5 top-level dirs, research needed, release intended, carryover ≥0.30 last session} → ultradeep; rule sits before carryover rule. Cost formula from subagents.jsonl token fields; baseline deep/5w median total_token_output 3,362,514 (n=49; max 2,013,195,505 is corrupt), total_agents median 12 (n=744); economical if overhead ≤3.5× at ≥2× closed issues; re-measure after 3 runs. 9 EARS ACs + 6 named tests (TV-001). Migration: profile block parity-exempt (like skill-evolution), drift-check Check 6 unaffected, historical records untouched.
## Evidence: 3,262 clean sessions.jsonl records / 46 files; session types housekeeping 1,991 · deep 1,105 · feature 94 · unset 70; wave histograms deep 5:744 6:46 7:6 8:1; events.jsonl session.started carries NO session_type (3,199× unset) → type distribution only from sessions.jsonl; this repo has no sessions.jsonl.
## Open questions
1 does server validate.mjs whitelist keys (drops unknown session_profile?) — check before merge. 2 command consolidation → sunset-review data. 3 this repo's missing sessions.jsonl. 4 corrupt token max. 5 model defaults unmeasured (A/B over 2 runs). 6 what are the 1,877 waveless housekeeping records (autopilot stubs? backfill? express)? (d8: backfill artefacts.)
STATUS: done
