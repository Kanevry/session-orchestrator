# d10 — Codex/Astra, Cursor, Pi, Copilot — 2026-09-06
## Summary
1 Codex: host 0.144.4, latest stable 0.153.4 (09-04) — 9 minors behind. Codex documents 12 hook events; our hooks-codex.json has 6 (SessionStart, PreToolUse [], PostToolUse, SubagentStart [], SubagentStop, Stop) → missing SessionEnd, UserPromptSubmit, PermissionRequest, PreCompact, PostCompact, Interrupt (0.150.0). tech.md claim "Claude-only events remain absent" is FALSIFIED — SessionEnd exists on Codex → GH#66 release half wireable (#1171 resolvable). NB SessionEnd/Interrupt timeout default 1 s, max 3 s → on-session-end must fit or run async (0.148.0 async hooks).
2 Codex sets PLUGIN_ROOT + PLUGIN_DATA and CLAUDE_PLUGIN_ROOT/CLAUDE_PLUGIN_DATA compat aliases; 0.149.0 "run hooks with captured session environment" → GH#64 addressed upstream. UUIDv7 documented since 0.143.0 (#27714), item ids 0.144.0 → GH#66 is the contract, not a bug.
3 0.149.0 #39068 REMOVED skill model delegation → `model-preference-codex: gpt-5.4` in 20 SKILL.md is dead; 0.153.4 Astra is bundled default. 0.151.0 #41208 likely fixes #1226 (re-measure ≥0.151). 0.153.0 remote-marketplace CLI first-class (#42150) → re-evaluate #826 short-form (codex plugin marketplace add owner/repo[@ref] verified on 0.144.4). 0.152.0 planning tool default off. Skill catalog token budget (0.149) + response budgets (0.150) → 50 skill descriptions compete (longest 1,012 chars, sunset-review).
4 Codex subagents native (multi_agent stable; TOML agents in ~/.codex/agents or .codex/agents; agents.max_concurrent_threads_per_session; collaboration namespace spawn_agent/list_agents/wait_agent); plugin-shipped agents undocumented. Codex reads skills/*/SKILL.md via .codex-plugin/plugin.json (skills may be array since 0.142).
5 GPT-6 Astra: 1,050,000 ctx, 922k in / 128k out, $10/$1 cached/$50 per 1M, effort low|medium|high|xhigh|max, tools incl. patch application + computer use; first "Critical" cybersecurity capability under Preparedness; Codex 1.9× faster vs GPT-5.6 Sol. Implication: add `codex:<model>[@effort]` channel to agent-mapping (config.mjs accepts only session-orchestrator:/cursor:/ssh:; foreign-dispatch.mjs implements cursor-agent only) — cheaper/safer than cursor channel (local `codex exec -c model=… -c model_reasoning_effort=…`), keep detached worktree + mandatory review.
6 Cursor: 18 agent hook events (we wire 11; unused beforeMCPExecution, beforeReadFile, beforeSubmitPrompt, preCompact, afterAgentResponse/Thought); skills per agentskills.io from .agents/skills, .cursor/skills, legacy .claude/skills/.codex/skills; plugin system: root plugin.json ($schema agent-plugins.org/schemas/1.0.0) OR .cursor-plugin/plugin.json — we have NEITHER (biggest Cursor gap); AGENTS.md read; CLAUDE.md NOT mentioned in Cursor rules doc (docs/cursor-setup.md claim unbacked). Our Cursor adapter is real (11 events, deny via permission). origin/feat/cursor-native-adapter fully merged (is-ancestor true) → delete.
7 GENERATOR BUG: scripts/generate-cursor-adapter.mjs:109 writes argument-hint UNQUOTED → 24/28 .cursor/commands/*.md carry the GH#54 array shape again (commands/ and pi/prompts/ are clean 27/28). check-cursor-adapter.mjs only runs generator --check (generator vs generator) → invisible by construction. One-line fix (yamlQuote on argument-hint) — do it now regardless of #726.
8 Pi: npm @earendil-works/pi-coding-agent 0.85.1 (09-05); manifest pi:{extensions,skills,prompts,themes} — ours conform; `pi install git:github.com/user/repo@ref` documented upstream (#826 Pi half: syntax verified, execution against our repo still open); extension API ~30 events, we use 5 (session_start, session_shutdown, tool_call, tool_result, agent_end); no subagents documented → agents-per-wave inert on Pi. Worth adding: project_trust, turn_start/end, thinking_level_select, session_before_compact. Host "pi 2026.07.23" is a different version scheme; not on agent PATH.
9 Copilot CLI: reads AGENTS.md, .github/copilot-instructions.md AND CLAUDE.md (@include); skills from .github/skills, .claude/skills, .agents/skills (name lowercase-hyphen, description required, optional license, allowed-tools); hooks .github/hooks/*.json with 7 events (sessionStart, sessionEnd, userPromptSubmitted, preToolUse, postToolUse, errorOccurred, agentStop); blocking semantics undocumented (reference page 404) → hooks not responsible yet; minimal support = root AGENTS.md + .agents/skills path (1 file + 1 symlink/generator + parity test).
10 agentskills.io conformance over 50 SKILL.md: 0 hard violations; 13 non-spec top-level keys (model 49, user-invocable 28, tags 26, model-preference 22, model-preference-cursor 20, model-preference-codex 20 [dead], color 8, tools 7 [spec: allowed-tools], args-schema 5, derived-from 3, upstream-url 3, disable-model-invocation 2, attribution 1); spec suggests `metadata` (string→string) for extras; risk today low, tomorrow GH#54-class.
11 Other harnesses: Gemini CLI extensions use skills/<name>/SKILL.md 1:1 but GEMINI.md not AGENTS.md; OpenCode reads AGENTS.md + ~/.claude/skills; Amp reads AGENTS.md + .agents/skills/.claude/skills; Kiro AGENTS.md (skills unclear). agent-plugins.org 1.0.0 (TSC: Amazon, Cursor, Microsoft, OpenAI, Vercel): root plugin.json + skills/ + mcp.json — we have 0 references.
12 LCD layer (7 of 8 harnesses read AGENTS.md and/or skills): (a) root AGENTS.md — MISSING today (only agents/AGENTS.md authoring spec); this repo opened in Codex cannot find its own Session Config; symlink AGENTS.md → CLAUDE.md preferred (drift-check Check 7 must treat symlink as identical-by-construction; Windows caveat) vs thin alias; (b) .agents/skills/ generated wrappers (Copilot+Cursor+Amp); (c) root plugin.json per agent-plugins.org 1.0.0 (same data as marketplace.json + codex plugin.json) — the only LCD item with new value. Hooks stay adapter work (4 deny conventions).
13 Generator (#726): build as 4→1 with a `--check` parity gate that validates artefacts against the SPEC (argument-hint string, agentskills frontmatter, event names vs matrix), not generator-vs-generator. Source hooks/hook-graph.json {id, script, capability, matcher, timeout, blocking} with harness-neutral capabilities (session.start/end, tool.pre/post/failed, shell.pre/post, file.edited, prompt.submitted, turn.stop, turn.interrupted, subagent.start/stop, compact.pre/post, cwd.changed) + hooks/harness-matrix.json (capability → native event, deny envelope, plugin-root env). Current manifests: claude 10 events, codex 6, cursor 11, pi 5.
## Open questions
1 can a Codex plugin ship agent definitions? 2 Copilot hook blocking semantics (404). 3 #1226 fixed on ≥0.151? 4 Pi host version mapping. 5 does Cursor read CLAUDE.md? 6 SessionEnd 3 s budget on Codex vs on-session-end.mjs (221 ms measured by d3 — fits). 7 model-preference-codex/cursor (20+20 files): delete or move to metadata.
## Sources (2026-09-06): gh api openai/codex releases p1-3; learn.chatgpt.com/docs/{hooks,plugins,build-plugins,agent-configuration/subagents}; developers.openai.com/api/docs/models/gpt-6-astra (openai.com/index/gpt-6-astra 403 → search snippet); cursor.com/docs/{agent/hooks,context/rules,context/skills,cli/overview,plugins}; pi.dev/docs/latest/{packages,extensions}; docs.github.com copilot-cli (hooks-reference 404); agentskills.io/specification; agent-plugins.org; opencode.ai/docs/rules; ampcode.com docs; kiro.dev/docs/steering; geminicli.com/docs/extensions.
STATUS: done
