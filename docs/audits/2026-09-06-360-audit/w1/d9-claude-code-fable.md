# d9 — Claude Code 2.1.200→2.1.263 + Fable 5.1 — 2026-09-06
## Summary
- 62 deltas: 1 retire-ours, 28 adopt, 25 complement, 8 ignore. No native feature fully replaces a moat component.
- THREE always-on rules now state falsehoods: CSM-005 (telemetry env vars kill the channel; Windows lacks it) — falsified by 2.1.248 (CSM on Bedrock/Vertex/Foundry and with telemetry disabled) + 2.1.239 (Windows); CSM-004 "fails quietly" half obsolete (refused/dropped/too-large reported to sender since 2.1.235–.238; offline RC session → "queued" 2.1.261); LM-003 (non-Anthropic providers run bare /loop on fixed 10-min schedule) — lifted by 2.1.248.
- `claude plugin eval` exists on this host and duplicates evals/instruction-ablation/run.mjs (336 lines) exactly: --ablation with-without, --runs, --threshold, --judge-model (haiku), --max-cost-usd, --mocks record|off, --json, HTML report; eval dir from plugin.json `experimental.evals` else evals/; cases as <dir>/**/case.yaml or prompt.md + graders/*.md. → RETIRE run.mjs (port one case first: psa-003-foreign-work). /eval (session-process rubric) has no native counterpart → keep.
- FA-6 caught up: non-teammate agent spawns default to background in interactive sessions (2.1.232); 200-spawn cap removed (2.1.224); concurrency cap 20 (CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS, 2.1.217); nesting depth 3 (2.1.219); subagent at maxTurns returns partial + SendMessage continuation hint (2.1.246); bashOutputMaxChars/taskOutputMaxChars up to 128K (2.1.261). Our 3–4 batches vs deep:18 are over-conservative; wave-loop rationale must be rewritten (platform default, not our deviation).
- MODEL DEFAULT PREMISE FALSE: agents/*.md = 11 inherit, 3 haiku (skill-applied-judge, dialectic-deriver, eval-judge), 1 opus (ux-evaluator). Skills model: 19 inherit / 17 sonnet / 13 haiku. Recommend: ux-evaluator opus → inherit + effort: high (2.1.251: agent-definition model now OVERRIDES CLAUDE_CODE_SUBAGENT_MODEL); introduce `effort:` (agent + skill field, unused) instead of family pins; check haiku pins (200K ctx, no effort) for transcript-reading judges; do NOT blanket-pin Fable 5.1 (Anthropic: start with Opus 5 … use Fable when evals still fall short; subagents run on 5-min cache TTL → cold contexts pay cache WRITES $12.50/MTok on Fable; the cheap $0.25 cache READ only pays on read-dominated traffic) → measure with `claude plugin eval --model claude-fable-5-1` first. Gotcha: 2.1.260 fixed `model: fable` ignoring [1m] tag → silent 200K.
- Fable 5.1: $10/$50, cache read $0.25, write $12.50/5m $20/1h, 1M/128K, adaptive thinking always on, default effort high, cutoff Jun 2026, release 2026-09-01; Terminal-Bench 4.0 55.8 % (Mythos 60.9 %), OSWorld 41.7 %, CursorBench 73.4 % max effort; "25 % cheaper than Fable 5 typical, ~45 % agentic". Announcement names no ASL level; system card not read. Opus 5 $5/$25 1M; Sonnet 5 $2/$10 1M; Haiku 4.5 $1/$5 200K no effort, retirement ≥2026-10-15.
- Hooks: 31 documented events, we register 10 (unused: PreCompact/PostCompact, InstructionsLoaded, ConfigChange, PermissionRequest/Denied, WorktreeCreate/Remove, UserPromptSubmit, StopFailure, Setup, FileChanged, PreModelSwitch/PostModelSwitch, DirectoryAdded); hook TYPES http/mcp_tool/prompt/agent besides command — `type: prompt` is the native form of pre-auq-clarity + discovery-validator adjudication (and the documented alternative to #1109 observer). 2.1.214 exit-2 blocks even if stdout JSON fails validation; 2.1.248 invalid stdout JSON → hook error; 2.1.210 hook timeout no longer misreported as user rejection (unattended sessions); 2.1.207 ${user_config.*} in shell form rejected. SessionStart resume payload carries staleness + re-cache cost (2.1.251).
- Plugins: `claude plugin details <name>` (component inventory + projected token cost), `/skill-doctor` (2.1.252+, unused skills + context cost), `claude plugin validate --json` (2.1.259), plugins from /plugin activate immediately (2.1.221), `claude plugin update <bare-name>` (2.1.246), archive/command sources, strictKnownMarketplaces.
- Skills: 2.1.259 frontmatter `model:` on skills/commands was IGNORED in interactive sessions → now honoured (our 30 model: lines become live); context: fork defaults to background (2.1.218); unused native fields when_to_use (dedicated trigger slot; description truncated at 1,536 chars in listings), argument-hint, arguments, effort, paths, metadata, compatibility, disallowed-tools, agent, hooks, shell; agentskills.io: outside Claude Code only name/description/license/compatibility/metadata/allowed-tools allowed.
- Conformance (validate . --json: success; --strict on plugin.json: warning "CLAUDE.md at plugin root not loaded as project context" → strict CI gate would be red): (1) marketplace.json description+version sit under metadata (documented top-level; only metadata.pluginRoot exists) → dead config; (2) 7 SKILL.md declare `tools:` (agent field) instead of allowed-tools — silently ineffective (write-executable-plan, eli5, tmux-layout, brainstorm, grill, journey-audit, debug); (3) 8 undocumented skill keys (tags 26, model-preference 22, model-preference-cursor 20, model-preference-codex 20, color 8, args-schema 5, upstream-url 3, derived-from 3) → move under metadata:; (4) args-schema is ours (native: arguments + argument-hint); (5) agent keys sandbox-tier (15), output-schema (11) ours — output-schema ≈ ReportFindings; (6) no references/ dirs; (7) hooks.json uses only type command; PostToolBatch declared twice; (8) plugin.json lacks displayName + experimental.evals.
- Overlap verdicts: (a) ListAgents vs session-lock → complement (no repo/branch/mode/wave/heartbeat; not durable; subagents can't load it) but CSM-005 must be corrected; (b) native memory vs learnings → complement (machine-local, unstructured, 200 lines/25 KB, not loaded into subagents); adopt subagent `memory: project|local` for session-reviewer; (c) plugin eval → retire run.mjs, keep /eval; (d) /code-review + ReportFindings vs session-reviewer/persona-panel → complement + adopt ReportFindings as reviewer output contract + `/code-review --comment` posts GitLab MR notes via glab (2.1.257) replacing hand-built notes in gitlab-ops; (e) Workflow vs wave-executor → Stay holds but ADR-0010 premises fell (prompt 5.7k→1k, caps removed; workflowSizeGuideline advisory; OTel workflow.run_id; prefix stagger for cache reuse adoptable for waves; agent({schema}) prevalidation) — rewrite rationale; (f) auto-compact (Opus/Fable near 1M, 2.1.260) + project-root CLAUDE.md survives compaction → STATE.md must carry only what no re-read file carries.
- Other adopts: permission mode "default" renamed "Manual" (2.1.200); defaultMode bypassPermissions in .claude/settings*.json ignored (2.1.257); `--permission-prompts none` for unattended (2.1.259) → autopilot; `--restricted` (2.1.248) → sandbox-tier; promptCacheTtl/subagentPromptCacheTtl (2.1.243); /cost + statusline prompt_cache with miss cause (2.1.251/.260); TodoWrite/TaskCreate etc. unavailable on Opus 4.8/Sonnet 5/Fable 5/Mythos 5 (2.1.233, CLAUDE_CODE_ENABLE_TODO_TOOLS=1) — 3 files reference them (wave-loop.md, pre-task-scope-disjoint.mjs, platform-tools.md) → check; /goal check-ins 30m→1h→2h max 3 (2.1.234–.246), LM-008 lacks these; /loop usage breakdown (2.1.243); Monitor treated like Bash in auto mode (2.1.236); `claude -p` waits for armed Monitor (2.1.257); worktree isolation applies to Bash too (PSA-007 still needed for same-checkout index race); notify_when_idle on cross-session SendMessage (2.1.236); subagent SendMessage replies go to parent (2.1.248) confirms CSM-001.
## Patterns to copy
1 deferred tool schemas → rule INDEX + read-on-demand for wave prompts (rule-loader); 2 hook types prompt/agent instead of regex adjudication (pre-auq-clarity, discovery-validator); 3 ReportFindings as reviewer contract (AGENTS.md authoring line); 4 /skill-doctor + plugin details as sunset-review Phase 1 input (measure, don't re-derive); 5 when_to_use + paths: frontmatter sweep over 50 skills, move extras under metadata, fix tools: → allowed-tools.
## Open questions
1 Fable 5.1 system card unread (ASL). 2 claude plugin eval not executed (early-access gate? cost? publish default → --no-publish?). 3 is the --strict CLAUDE.md warning suppressible? 4 observer (#1109) still undocumented; prompt-hook is the documented route. 5 TodoWrite refs in 3 files. 6 /simplify no changelog delta.
STATUS: done
