# DO NOT UNTRACK. This file is deliberately tracked (owner decision 2026-07-21).
#
# The vault-side generator reads it to regenerate the project's _overview.md; if it
# leaves the repo, that registration breaks. Untracking has already been proposed and
# reverted twice — the privacy argument does not hold, because the GitLab path below
# is public elsewhere in this repo anyway (CHANGELOG.md, docs/ci-setup.md), so
# removing this file costs the registration and buys nothing. It passes
# check-owner-leakage CP1–CP10 as-is.

apiVersion: vault.gotzendorfer/v1
kind: Repository

metadata:
  name: session-orchestrator
  slug: session-orchestrator
  tier: top
  owner: bernhard

spec:
  techStack: [bash, markdown, claude-code-plugin]
  status: production
  tags: [plugin, orchestration, infrastructure, ai-agent]
  summary: "Claude Code plugin for multi-wave session orchestration with role-based agent dispatch, discovery, planning, scope enforcement, and evolve primitives."

  links:
    gitlab: infrastructure/session-orchestrator
    docs: docs/USER-GUIDE.md

  syncFiles:
    - CLAUDE.md
    - README.md
