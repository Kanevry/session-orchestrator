---
name: release
description: "Cut a release — the order the steps must run in, and the criteria that abort a release"
disable-model-invocation: true
---

# release

Canonical skill: `skills/release/SKILL.md`

Read that file and follow it exactly. Resolve relative links against `skills/release/`, not this wrapper.

Cursor has no Skill tool. Treat "invoke the release skill" as: Read `skills/release/SKILL.md`.
