# Session Orchestrator Plugin

> Project-instruction file resolution: this is `CLAUDE.md` on Claude Code / Cursor IDE; the equivalent file on Codex CLI is `AGENTS.md`. See [skills/_shared/instruction-file-resolution.md](skills/_shared/instruction-file-resolution.md).

> Für Installation, CLI-Nutzung und Architektur siehe [`README.md`](./README.md). Diese Datei enthält nur die runtime-kritischen Abschnitte, die von `skills/_shared/config-reading.md` gelesen werden.

## Structure

See [`README.md`](./README.md#components) for the canonical inventory (28 skills, 10 commands, 10 agents, 10 hook event matchers / 10 hook handlers). Runtime layout: `skills/`, `commands/`, `agents/`, `hooks/`, `.orchestrator/policy/`, `.claude/rules/`. Stable product/tech/structure context lives at `.orchestrator/steering/{product,tech,structure}.md` and is injected at session-start Phase 2.6 (when present).

## Destructive-Command Guard

`hooks/pre-bash-destructive-guard.mjs` blocks destructive shell commands in the main session (alongside subagent waves). Policy lives in `.orchestrator/policy/blocked-commands.json` (13 rules). Bypass per-session via Session Config:

```yaml
allow-destructive-ops: true
```

Rule source of truth: [`.claude/rules/parallel-sessions.md`](.claude/rules/parallel-sessions.md) (PSA-003). See README § Destructive-Command Guard for the full narrative.

## Agent Authoring Rules

Agent files live in `agents/` as Markdown with YAML frontmatter. Required fields:

```yaml
---
name: kebab-case-name          # 3-50 chars, lowercase + hyphens only
description: Use this agent when [conditions]. <example>Context: ... user: "..." assistant: "..." <commentary>Why this agent is appropriate</commentary></example>
model: inherit                 # inherit | sonnet | opus | haiku
color: blue                    # blue | cyan | green | yellow | magenta | red
tools: Read, Grep, Glob, Bash  # COMMA-SEPARATED STRING, not JSON array!
---
```

**Critical pitfalls** (cause "agents: Invalid input" validation failure):
- `tools` MUST be a comma-separated string (`Read, Edit, Write`), NOT a JSON array (`["Read", "Edit"]`)
- `description` MUST be a single-line inline string, NOT a YAML block scalar (`>` or `|`). Put `<example>` blocks inline.
- All 4 fields (name, description, model, color) are required. `tools` is optional.

Reference: https://github.com/anthropics/claude-code/blob/main/plugins/plugin-dev/skills/agent-development/SKILL.md

## Current State

- **2026-05-08 deep-2 — Discovery-derived 6-issue cluster (#344 #345 #346 #347 #348 #349):** 5W×6A coordinator-direct in `main`, isolation:none enforcement:warn cap=6. Discovery-driven session: backlog post-v3.4.0 had only tracking-only / data-gated issues, so W1 ran 6 parallel Explore probes (architecture, test-coverage, doc-drift, hooks-config, tech-debt, perf-health) surfacing 38 candidate findings; coordinator curated 6 disjoint-file-scope issues. **W1 Discovery (6 parallel Explore agents)** mapped per-issue scopes: D1 #344 6 createHash sites at exact line numbers + 4-export API design (digestSha256Short / digestSha256 / digestSha256WithSalt / digestMultiBufferSha256); D2 #345 8 config/ modules with 14 throw paths + per-module test-case lists (~112 target); D3 #346 5+1 vault-mirror submodules with isolation seams (~78 target); D4 #347 validator design + DOCUMENTED_ASYMMETRIES constant (9 cursor-missing + 2 cursor-only); D5 #348 byte-precise line refs for 4 doc edits + Cursor IDE facts; D6 #349 events-rotation top-level test surface (16-18 target). **W2 Impl-Core (6 parallel writers, disjoint paths)**: B1 #344 ships `scripts/lib/crypto-digest-utils.mjs` (~90L, 4 named exports with JSDoc) + 6 callers migrated (session-registry keeps `crypto` import for `randomBytes` at L84). B2 #345 ships 8 NEW test files in `tests/lib/config/` — **221 tests** (target 112, +97%; coercers alone 77 covering all 11 exports + every throw + override syntax `"6 (deep: 18, fast: 4)"`). B3 #346 ships 5 NEW test files in `tests/lib/vault-mirror/` — **148 tests** (target 78, +89%; utils 48 + render-learnings 32 + render-sessions 36 + auto-commit 11 + process 21). B4 #347 ships `scripts/lib/validate/check-hooks-symmetry.mjs` (136L, 4 checks) + 2-line wire into `scripts/validate-plugin.mjs` after L123 (validate-plugin grows 22→27 PASS). B5 #348 applies 4 doc edits: README L367 test count `2623+` → `3138+` + NEW `### Cursor IDE Support` section after Platform Support / `docs/prd/2026-04-21-vault-docs-orchestration.md` L5 `Status: Draft` → `Status: Complete (2026-05-01, Epic #229 closed)` / CLAUDE.md `2026-04-30 main-2026-04-30-1635` bullet stale parenthetical replaced with `[RESOLVED 2026-05-08 in v3.4.0: ...]` (note: D5 had referenced non-existent `docs/cursor-setup.md` — B5 omitted that link). B6 #349 ships `tests/lib/events-rotation.test.mjs` — **25 tests** (target 16-18, +56%); used real-fs injection via `chmodSync(dir, 0o555)` instead of vi.spyOn (blocked on ESM named exports — confirmed with existing `tests/unit/events-rotation.test.mjs` pattern). Inter-wave Quality-Lite caught 5 lint errors (3 unused imports + 2 unused-var declarations in B3 vault-mirror tests) — fixed coord-direct via `_path` removal + `_mkdirSyncSpy` rename. **W3 Impl-Polish (3 parallel agents — under-cap, no invented work)**: C1 ships `tests/lib/crypto-digest-utils.test.mjs` — **29 tests** (target 22-25; hardcoded hex literals computed externally via `node -e`, no tautological computation, all 4 exports covered with happy + edge + error). C2 ships `tests/scripts/check-hooks-symmetry.test.mjs` — **30 tests** (target 12-15, +100%; 13 fixture-based scenario groups × 2 cases + Results-line regex match). C4 simplifier passes on B1+B4 production: crypto-digest-utils unchanged (already clean), check-hooks-symmetry collapsed 3 try/catch JSON-loading blocks into `loadJson(filePath, required)` helper (-9L net). **W4 Quality (2 read-only reviewers + Full Gate sequential)**: Q1 architect-reviewer verdict SHIP for both modules — 3 low-severity findings on crypto-digest-utils (speculative `encoding`/`length` options unused by callers, 2 single-caller exports earn keep as a "family interface" preventing re-leak of `createHash` to callers) + 1 medium leaky-abstraction finding on check-hooks-symmetry (extractHandlers regex matches both `$CLAUDE_PLUGIN_ROOT` and `$CODEX_PLUGIN_ROOT` prefixes; no current exploit but flagged as future tracking). Q2 qa-strategist surfaced 2 HIGH PRE-EXISTING gaps in `scripts/lib/vault-mirror/process.mjs` (unknown-generator branch L106-111 untested + processSession session-id mismatch L213-223 — both pre-date this session, ~10 min follow-up filed for next session) + 0 anti-patterns. Full Gate (coord-direct sequential): typecheck 66/66, lint 0, tests **3591 passed / 12 skipped / 0 failed** (was 3138, **+453 tests**), validate-plugin 27/27. **W5 Finalization (coord-direct)**: CHANGELOG `[Unreleased]` populated with 4 sections (Added/Tests/Changed/Quality), CLAUDE.md narrative bullet (this entry), README test count bumped `3138+` → `3591+`, no version bump (purely additive internal work — no user-facing features), final commit + push GitLab origin + GitHub mirror, 6 issues closed with shipping-note comments. Net delta: **2 NEW production files** (crypto-digest-utils.mjs + check-hooks-symmetry.mjs), **16 NEW test files** (8 config + 5 vault-mirror + 1 events-rotation + 1 crypto-digest + 1 hooks-symmetry), 6 modified production callers + 1 wire-line in validate-plugin.mjs + 4 modified docs (README + PRD + CLAUDE + CHANGELOG). Total parallel-agent dispatches: 6+6+3+2 = 17 + Full Gate sequential. <!-- consistency:exempt:alias-phrasing -->

- **2026-05-08 PM deep — v3.4.0 release (#327 #328 #330 #342 #332 #325 #326):** 5W×6A coordinator-direct in `main`, isolation:none enforcement:warn cap=6. **W1 Discovery (6 parallel Explore agents)** mapped: #327 validator.mjs lines 105-174 procedural CLI parsing + lines 399-478 error collection + vendored schema lines 43-102 between BEGIN/END GENERATED SCHEMA sentinels; #328 zod schema has 8 type values + 7 status values + 4 required fields, wave-loop.md lines 135-155 is the agent-prompt construction seam; #330 sessions.jsonl p95=2.62h (76 entries) → 4h TTL validated, Phase 1.5 acquire / Phase 4.3 release sites identified; #342 4 of 5 event names UNVERIFIED in codebase (only SubagentStop already exists, on-stop.mjs handler), schema neighbour `learnings.mjs` (Zod-free) selected for subagents-schema; #332 Express Path is structural (housekeeping + ≤3 issues), context-pressure is complementary penalty term; #325 5-way split confirmed (config-parser separated as 5th sub-module, not 4-way); #326 telemetry split confirmed clean (4 functions extractable). **W2 Impl-Core (6 parallel agents)**: #327 ships `scripts/lib/vault-sync-baseline.mjs` (152L, 4 exports: computeSchemaHash 8-char SHA-256 prefix / writeBaseline atomic / readBaseline null-on-missing / diffBaseline by triple key) + validator.mjs CLI extension (`--mode=baseline|diff|full` additive to legacy hard|warn|off). #328 ships `skills/frontmatter-guard/SKILL.md` (126L) + `scripts/lib/frontmatter-guard.mjs` (240L, 4 exports incl. mtime-cached schema reader). #330 ships `scripts/lib/session-lock.mjs` (215L, 6 exports incl. acquire/forceAcquire/release/checkStale/readLock with cross-host pidAlive:null behaviour, atomic temp+rename) + `.gitignore` extension. #342 part 1 ships `scripts/lib/subagents-schema.mjs` (Zod-free, 8 exports mirroring learnings.mjs) + 2 hooks (post-tool-failure-corrective-context.mjs cap-20 corrective_context array, post-tool-batch-wave-signal.mjs deterministic batch signal). #332 ships `computeContextPressure(signals)` in mode-selector.mjs (3-component 0-1 score, level low/medium/high) integrated into `computeDelta()` as additive penalty (high → feature −0.15 / housekeeping −0.10, low → feature +0.05). #325 ships 5 sub-modules under `scripts/lib/ecosystem-wizard/{ci-detector,package-manager-detector,config-parser,config-writer,wizard-prompt}.mjs` + barrel re-export (12 public symbols, 56 existing tests pass byte-equivalent). **W3 Impl-Polish (6 parallel agents)**: #327 ships SKILL.md mode docs (vault-sync + wave-executor cross-linked). #328 ships wave-loop.md `Pre-Dispatch: Frontmatter-Guard Injection` step (gated on `detectVaultTaskScope`). #330 ships `session-start` Phase 1.2 acquire+AUQ recovery (active/stale-pid-dead/stale-pid-alive branches with cross-host Abort recommendation) + `session-end` Phase 3.8 release + `commands/close.md` note. #342 part 2 ships 2 more hooks (subagent-telemetry.mjs writes to NEW `.orchestrator/metrics/subagents.jsonl`, cwd-change-restore.mjs cap-20 cwd_changes) + `hooks/{hooks,hooks-codex}.json` registers all 4 new event keys (SubagentStop extended additively, on-stop.mjs preserved) + NEW `scripts/migrate-subagents-jsonl.mjs` v3 migrate-CLI clause + CLAUDE.md `## Structure` 6→10 hook event matchers / 6→10 handlers. #332 ships `phase-7-5-mode-selector.md` Step 4.5 AUQ annotation table for medium/high pressure levels. #326 ships `scripts/lib/autopilot-telemetry.mjs` (extracts writeAutopilotJsonl/defaultRunId/readHostClass/finalizeState; root file 575L→507L; 73 existing autopilot tests pass unchanged via re-export barrel). **W4 Quality (4 test-writers + simplifier + Full Gate runner)**: 196 new vitest cases across 8 NEW test files (vault-sync-baseline 25 + frontmatter-guard 31 + subagents-schema 29 + 4 hook tests 12 + session-lock 16 + mode-selector-context-pressure 13 + ecosystem-wizard-modules 68 + autopilot-telemetry 27 + delta = 196 net). Simplifier (W4-D5) edited 4 production files: hoisted dynamic import in post-tool-failure hook, `pad2` → `String.padStart` in autopilot-telemetry, extracted `safeBootstrapLock` + collapsed nested ternary to if/else chain in mode-selector, consolidated 3 nested ternary chains in subagent-telemetry hook via `firstNonEmptyString` helper. Full Gate (W4-D6) caught 2 production bugs in `frontmatter-guard.mjs` fixed coord-direct: WRITE_INTENT_RE regex stem boundary (`generat` → `generat[a-z]*` for write/creat/emit/mirror/updat/add/insert) + trailing-newline cosmetic on snippet generator. **W5 Finalization (coord-direct)**: CHANGELOG `[Unreleased]` consolidated into `[3.4.0] - 2026-05-08` block (5× ### Added + 2× ### Changed + 1× ### Quality entries for #327 #328 #330 #342 #332 #325 #326, preserving all prior unreleased work since v3.3.0); 5 metadata bumps `package.json` 3.3.0→3.4.0, both `plugin.json` 3.3.0→3.4.0, both `hooks*.json` banner v3.3.0→v3.4.0; CLAUDE.md narrative bullet (this entry); 7 issues closed with shipping notes; final commit + tag v3.4.0 + push GitLab origin + GitHub mirror + `gh release create v3.4.0 --latest`. **Quality**: typecheck 65/65 OK (was 56, +9 NEW modules), lint 0 errors (4 inline coord fixes during W4: 2 eqeqeq + 2 unused-vars), tests **3138 passed / 12 skipped / 0 failed** (was 2942, **+196 tests**), validate-plugin 22/22 OK. Net delta: 11 NEW production files (vault-sync-baseline + frontmatter-guard + session-lock + subagents-schema + autopilot-telemetry + 5 ecosystem-wizard sub-modules + four new hooks + migrate-subagents-jsonl), 1 NEW skill (`skills/frontmatter-guard/`), 8 NEW test files, ~14 modified files (.mjs + .json + SKILL.md + .gitignore). <!-- consistency:exempt:alias-phrasing -->

- **2026-05-08 deep — Vault/Metrics-Cleanup Bundle (#321 #323 #343 #329 #322 #324):** 5W×6A coordinator-direct in `main`, isolation:none enforcement:warn cap=6. **W1 Discovery (6 parallel Explore agents)** mapped surfaces: #321 `validateSession()` already guards inversion at session-schema.mjs:175, but 4 invalid rows existed in sessions.jsonl (line 75 legacy `ended_at`+`duration_ms` schema, lines 69/71/76 monotonicity violations — including the 2026-05-07 deep session that re-triggered the bug yesterday); #323 1/129 records missing `expires_at`; #343 V1 vault-mirror line 88 `${platform}` template-coercion + missing `repo:` field; #329 validator.mjs (lines 104-143) only reads CLI args, never CLAUDE.md; #322 1 caller (tests/lib/worktree.test.mjs:423) of the dead re-export at worktree.mjs:421; #324 3 long validators with clean section breakpoints, 98 tests cover behaviour. **W2 Impl-Core (3 parallel agents, Track A writer-quality)**: #321 ships `clampTimestampsMonotonic` + `aliasLegacyEndedAt` in session-schema.mjs (+97L, pure no-throw no-mutate), wired into emit-session.mjs (+29L) before validateSession; STDERR `WARN session_id=...` on clamp/alias, STDOUT remains pure JSON; 4 invalid rows patched in-place (`unmappable: 4 → 0`); zero rogue writers detected (every JSONL write site already funnels through emit-session.mjs). #323 ships `LEARNING_TTL_DAYS` (frozen 9-entry per-type policy) + `deriveExpiresAt` in learnings.mjs; appendLearning auto-stamps; new `scripts/backfill-learnings-expires.mjs` CLI (dry-run safe, `--apply` patches + writes `<file>.bak.<isoDate>`, idempotent re-run, `_backfilled_expires_at: true` forensic tag); 1/129 records backfilled. #343 ships `fmLine()` skip-emit helper + `deriveRepo()` (parses git origin ssh+https, basename fallback, cached) in vault-mirror; both V1+V2 generators emit `repo:` frontmatter. **W3 Impl-Polish (3 parallel agents)**: #329 ships unconditional CLAUDE.md/AGENTS.md read in validator.mjs (via `resolveInstructionFile` + dynamic-import `_parseVaultSync`, try/catch silent fallback, CLI `--exclude` additive). #322 deletes the re-export at worktree.mjs:421 + splits the test caller into 2 cache-busted dynamic imports (cycle now uni-directional). #324 decomposes 3 validators into 17 module-private helpers (wave-resource-gate 2, session-schema 8 — preserving the W2 #321 additions, owner-config 7); 154 existing tests still pass byte-equivalent. **W4 Quality (3 test-writer agents + Full Gate)**: 68 new vitest cases (T1: 22 clamp + 8 alias + 6 emit-session integration + 11 V1/V2 frontmatter + 4 deriveRepo; T2: 6 deriveExpiresAt + 5 appendLearning auto-stamp + 7 backfill integration; T3: 7 vault-sync exclude integration). **W5 Finalization (coord-direct)**: CHANGELOG `[Unreleased]` adds 5× ### Fixed + 1× ### Changed entries (#321 #323 #343 #329 #322 #324) + .gitignore extension; CLAUDE.md narrative bullet; 6 issues closed with shipping notes. **Quality**: typecheck 56/56 OK, lint 0 errors, tests **2942 passed / 12 skipped / 0 failed** (was 2876, **+66 tests**); migrate-CLI dry-run sessions.jsonl unmappable: 4 → 0. Net delta: 1 NEW production file (`scripts/backfill-learnings-expires.mjs`), 2 NEW test files (`tests/scripts/backfill-learnings-expires.test.mjs`, `tests/skills/vault-sync-exclude.test.mjs`), 10 modified production files (.mjs + 1 SKILL.md + .gitignore), 5 modified test files. Simplification pass intentionally skipped (refactor in #324 already a behaviour-preserving simplification; rest is targeted writer-fixes). <!-- consistency:exempt:alias-phrasing -->

- **2026-05-07 deep — Benchmark P1-P5 adoption epic (#336 #337 #338 #339 #340):** 5W×6A coordinator-direct in `main`, isolation:none enforcement:warn (auto-promoted strict on Discovery + Quality phases) cap=6. **W1 Discovery (5 Explore agents)** mapped surfaces for all 5 issues — 6/13 rules already had `paths:` frontmatter (rename to `globs:`); rule-loader was NEW work; bootstrap-gate Phase 0 = single hook-point for auto-skill-dispatch; canonical agent YAML pitfalls quoted verbatim from CLAUDE.md; STATE.md persistence pattern (parallel to `docs-tasks`) for mission-status. **W2 Impl-Core (3 parallel agents)**: #338 ships 3 steering docs at `.orchestrator/steering/{product,tech,structure}.md` (~50L each, sourced from CLAUDE.md stable repo-facts) + new Phase 2.6 in session-start (silent no-op when dir absent) + new Step 7 in `/plan new`; #339 ships 3 reviewer agents (`architect-reviewer`/`qa-strategist`/`analyst`, comma-separated tools, all 4 YAML fields, validate-plugin 22/22) + new step 5a in wave-loop.md gated by `wave-reviewers: []` (default empty = no-op); #337 ships `skills/using-orchestrator/SKILL.md` (140L meta-skill, 23-entry phrase map de+en, score tiers 0.95/0.90/0.60/0.40, threshold 0.85, AUQ-disambiguate when delta < 0.15) + bootstrap-gate +7L documentation. **W3 Impl-Polish (2 parallel agents)**: #336 ships `scripts/lib/rule-loader.mjs` (NEW, picomatch-via-createRequire with inline `globToRegExp` fallback, parse-error → always-on graceful) + 6 `paths:` → `globs:` renames + `skills/_shared/config-reading.md` "Rule Loading (Glob-Scoped)" section (+45L); #340 ships `scripts/lib/mission-status-schema.mjs` (NEW, 5-value enum + transition validator with idempotent + rollback semantics) + `state-md.mjs` `parseMissionStatus`/`writeMissionStatus` (+60L) + `skills/session-plan/SKILL.md` machine-readable emission block (+33L) + `skills/session-end/SKILL.md` Phase 1.9 enum-based classification (+14L). **W4 Quality (4 agents)**: 3 test-writer agents shipped 149 new vitest cases (rule-loader 28, config-reading-glob 11, using-orchestrator 24, session-start-steering 9, persona-reviewers 9, mission-status-schema 36, state-md-mission-status 15, mission-status-enum-docs 17) + security-reviewer ran Full Gate. One alias-coverage regression caught + fixed coord-direct (`skills/using-orchestrator/SKILL.md:22` needed AGENTS.md mention). **W5 Finalization (coord-direct)**: CLAUDE.md adds 3 Session Config fields (`auto-skill-dispatch`, `persona-reviewers`, structure pointer at steering/), CHANGELOG `[Unreleased] ### Added` block, 5 issues closed with shipping notes. **Quality**: typecheck 56/56 OK (was 54, +2 new modules), lint 0 errors, validate-plugin 22 passed / 0 failed (10 agents now), tests **2875 passed / 11 skipped / 0 failed** (was 2727, **+148 tests**). Net delta: 7 NEW files (3 agents + 3 steering docs + using-orchestrator/SKILL.md + 2 lib modules), 11 modified files, ~250 production-line insertions, 149 test cases. Simplification pass intentionally skipped (only 3 production files changed; 2 NEW with no slop; resource pressure rising — 3.7 GB RAM @ 9 sessions). <!-- consistency:exempt:alias-phrasing -->

- **2026-05-07 discovery snapshot (read-only):** Live `glab issue list --per-page 30` returns **16 open** (was 5 after 2026-05-01 closures). New since 2026-05-02: 10× `[Discovery 2026-05-03]` cluster (#321–#330) + #332 `mode-selector context-pressure signal`. Active priority:high cluster (no branch yet, will trip stale at +3d): #321 sessions.jsonl unmappable + writer bug, #327 vault-sync baseline-diff, #328 Frontmatter-Guard skill, #330 distributed session-lock with TTL. Open epic-tracking only: #305 (cross-repo strict watcher), #297/#298 (autopilot data-gated, RUN-volumen-blocked), #213 (composio submission ship-ready), #123 (awesome-claude-code tracker). **Plugin-side dogfood gap closed:** `.npmrc` shipped this session with `ignore-scripts=true` + audit-level=high + engine-strict=true (npm-side equivalent of SEC-020 — pnpm-only directives like `block-exotic-subdeps`/`trust-policy` intentionally omitted because plugin uses npm). **Inventory parity confirmed:** README "26 skills, 6 hook event matchers, 6 handlers" is correct (skills/ has 27 dirs but `_shared/` is internal; hooks.json has 5 event keys with 6 matcher entries because PreToolUse splits into Edit|Write + Bash). Discovery `/discovery all` cycle 2026-05-07 also surfaced: untracked `.orchestrator/audits/` + `.orchestrator/drafts/` added to `.gitignore` (housekeeping artefact dirs); harness-audit `Persistence Health 5/10` due to 125 active learnings without `expires_at` already tracked as #323; 2 large-file refactor candidates (ecosystem-wizard 636L #325, autopilot 575L #326) already tracked as priority:low tech-debt. Online benchmark synthesis filed in memory `project_benchmark_2026-05-07.md`: top adoptable potentials are glob-scoped rules (Cursor `.cursor/rules/*.mdc`), auto-triggering skills (obra/superpowers `using-superpowers` meta-skill), `steering/` persistent context split (Pimzino), persona-expansion reviewer-agents (BMAD/superpowers), mission-status enum (ab-method); large-bet candidate is per-story worktree-pipelines as Autopilot Phase D (bmad-autonomous-development). <!-- consistency:exempt:alias-phrasing -->

- **2026-05-01 housekeeping (PM) — #305 cross-repo flip + en-masse residual archive:** Express Path activated (3 sequential coord-direct tasks, no waves, no parallel agents). **Task A** — `node scripts/run-migrate-v2-cross-repo.mjs --apply` walked all 16 ROLLOUT_REPOS: 9 had `learnings.jsonl`, 7 skipped (no JSONL). v2 clauses (scope coercion + `source_session` derivation) fixed **5** records (3 in clank, 2 in projects-baseline); remaining residual was **477 missing-insight + 2 malformed** (LeadPipeDACH JSON unparseable). **Task B (policy)** — user chose "archive en masse to `legacy.jsonl`" via AUQ. Wrote `.orchestrator/metrics/legacy/learnings-pre-v2-strict.jsonl` per repo with provenance header (`_provenance/_archived_at/_source_session/_reason`); per-repo split: launchpad-ai-factory 32 kept / 157 archived, GotzendorferV2 77 / 58, LeadPipeDACH 151 / 124 (incl. 2 malformed), clank 13 / 65, mail-assistant 55 / 36, n8n 0 / 3, projects-baseline 43 / 36. Aggregate **402 kept / 479 archived** across 9 repos with prior residual; EventDrop.at and GotzendorferAT had 0 residual (no archive file written). All 16 ROLLOUT_REPOS now at **0% invalid rate** in `learnings.jsonl` (DoD met for plugin + cross-repo data). **Task C** — `node scripts/promote-vault-strict.mjs --apply` flipped all 8 ELIGIBLE_REPOS + projects-baseline `CLAUDE.md.template` from `vault-integration.mode: warn` → `strict`: Codex-Hackathon `2e98b13`, EventDrop.at `74586c3`, GotzendorferAT `f93d794`, WalkAITalkie `f7d5dbf3`, ai-gateway `6c3ce07`, eventdrop-render-service `7cd53e2`, feedfoundry `2f4f909`, launchpad `dbd68fe`, projects-baseline (template) `2b62493` — 9 commits, no push (per script contract). #305 audit comment posted (note 31215) summarizing all three tasks; #305 kept open per the recommendation that 5 remaining repos (launchpad-ai-factory, GotzendorferV2, LeadPipeDACH, clank, mail-assistant) need a sessions-side canonicalization pass before they qualify for `ELIGIBLE_REPOS` extension. **Plugin source unchanged this session** — typecheck 54/54 OK, lint 0 errors, working tree clean throughout (only data writes in OTHER repos + STATE.md gitignored). Session ID `main-2026-05-01-housekeeping-2`. <!-- consistency:exempt:alias-phrasing -->

- **2026-05-01 deep session — #305 Migrate-CLI v2 ship + Epic #229 close-out:** 5 waves × 4 parallel agents under coordinator-direct orchestration in `main`, isolation:none enforcement:warn cap=4. **Cluster A — #305 plugin-learnings sanitation + warn→strict toolchain:** `scripts/lib/learnings.mjs` +20 LoC — two new clauses in `migrateLegacyLearning()`: scope-enum coercion (`vault-tools|deep-sessions|wave-executor|coordinator → local`) + `source_session ← sessions[0]` derivation when missing. 13 vitest cases in new `tests/scripts/migrate-learnings-v2.test.mjs` (scope coercion × 4 values + unknown-bad passthrough + source_session derivation × 4 + combined + idempotency). 5 REQUIRED_FIELDS contract regression tests added to `tests/lib/learnings.test.mjs` (validateLearning + appendLearning guards for insight + source_session enforcement, now 81 total `it()` blocks). New `scripts/promote-vault-strict.mjs` (469 LoC) + `tests/scripts/promote-vault-strict.test.mjs` (313 LoC, 8 vitest cases) — dry-run safe by default, `--apply` flag, per-repo `chore(orchestrator): promote vault-integration to strict mode — refs #305` commits, no push. D1 audit (133 records, 19 invalid): 9 deleted (truly empty), 5 archived (missing insight), 5 scope-coercion fixed → plugin `learnings.jsonl` at 0 invalid / 119 active records post-sanitation (down from 14.3%). **Cluster B — Epic #229 close-out:** D2 confirmed all 23/23 PRD items shipped on disk (Sub-Epic A `771b99c`, Sub-Epic B `356c4f9` baseline, Sub-Epic C `fff35f7`). All 23 PRD checkboxes flipped to `[x]` + new "Sub-Epic Status (2026-05-01)" block added to `docs/prd/2026-04-21-vault-docs-orchestration.md`. Epic #229 closed in W5. **Cross-repo readiness (D3):** 8 repos already pass DoD (Codex-Hackathon, EventDrop.at, GotzendorferAT, WalkAITalkie, ai-gateway, eventdrop-render-service, feedfoundry, launchpad), 2 pass after v2+archive (mail-assistant, n8n), 5 blocked by sessions-side invalid rates (launchpad-ai-factory, GotzendorferV2, LeadPipeDACH, clank, projects-baseline — out of scope this session). **Quality:** typecheck 54/54 OK, lint 0 errors, plugin tests 2697 → **2723** (+26: 13 v2 + 5 contract + 8 promote-strict). Backlog 6 → 5 open after #229 closure. <!-- consistency:exempt:alias-phrasing -->

- **Plugin version:** v3.2.0 (shipped 2026-04-27 to GitHub — consolidated stable supersedes v3.0.0-rc.1; bundles Windows native + env-aware sessions + Mode-Selector + Autopilot). Public release at https://github.com/Kanevry/session-orchestrator/releases/tag/v3.2.0.
- **Active epic:** #271 v3.2 Autopilot — Phase A + B shipped, Phase C-1 + C-1.b + C-1.c + C-2 + C-5 complete. **Phase C-5 #302 closed**: `scripts/autopilot.mjs` (NEW, 406L) — headless walk-away CLI driver. `--headless` required flag; `--verbose` pipes child stdio; `--dry-run` round-trips without spawning. Wires four DI seams to live impls: `modeSelector` → `selectMode(buildLiveSignals())` (learnings surfaced once pre-loop), `sessionRunner` → `child_process.spawn('claude', ['-p', '/session <mode>'])` with `AUTOPILOT_RUN_ID` env propagation + tail-1 sessions.jsonl read on exit, `resourceEvaluator`/`peerCounter` → `Promise.all(probe(), detectPeers())` cached snapshot. `tests/fixtures/claude` stub binary (60L) + `tests/scripts/autopilot.test.mjs` (5 integration tests via PATH-override) cover dry-run, missing-flag, happy-path 2-iteration `autopilot_run_id` correlation, failed-wave kill-switch, sessionRunner-throw kill-switch. `commands/autopilot.md` documents `--headless` + Headless Mode section. **v3.2 Autopilot epic effectively closed** — only #297 calibration (needs 10+ runs) + #298 evolve type 8 (needs runtime data) remain, both blocked on autopilot RUNS not code. Live walk-away `/autopilot --headless` now unblocked.
- **Stack:** Node 20+, vitest 4.1.5, ESLint 10. Run `npm ci` after cloning. Test: `npm test`. Lint: `npm run lint`.
- **Tests:** 2589 passed / 12 skipped as of 2026-04-30 PM deep session. Coverage above thresholds 70 / 65 / 70 / 60.
- **Backlog:** 12 open issues (verified 2026-04-30 main-2026-04-30-1635 deep session; down from 14 after closing #309 DDD-Trio-adoption + #271 v3.2 Autopilot epic — all phases A/B/C-1/C-1.b/C-1.c/C-2/C-5 shipped; sub-issues #297/#298 remain data-gated). #318 spawned earlier as G-axis tracker (Owner Persona baseline propagation). Remaining cluster: epics (#229), baseline-MR-blocked (#314, #315, #318), autopilot-RUNS-blocked (#297, #298), tracking-only (#213, #123, #305), plus a thin tail of priority:low chores.
- **GH#31 vault-mirror auto-commit (2026-04-27):** `scripts/vault-mirror.mjs` (+134L) gains an opt-in auto-commit phase triggered by `--session-id <id>`. After writing artifacts, stages `40-learnings/` + `50-sessions/`, runs per-file `_generator: session-orchestrator-vault-mirror@1` frontmatter check, commits as `chore(vault): mirror <id> — N learnings + M sessions` when staged set is all-mirror, unstages + warns + emits `auto-commit-skipped/non-mirror-staged-changes` action on mismatch, idempotent no-op on empty staged set. Eliminates 1+ day Vault-mirror-backlog pattern (76×40-learnings + 28×50-sessions @ 2026-04-27 vault discovery). 8 regression tests (`tests/unit/vault-mirror.test.mjs` describe-block "auto-commit (#31)"): happy/mismatch/idempotent/dry-run/no-commit/missing-dirs/backlog-catchup/non-git-vault.
- **GH#31 phased-rollout completion + #307 close (2026-04-27 PM):** `skills/session-end/session-metrics-write.md` Phase 3.7 step 5 (+1L) + `skills/evolve/SKILL.md` Step 9c (+3L: `EVOLVE_SESSION_ID="evolve-$(date -u +%Y-%m-%d-%H%M)"` derivation) now pass `--session-id` to vault-mirror, activating the auto-commit phase end-to-end. evolve commits land as `chore(vault): mirror evolve-<date> — N learnings + 0 sessions`. **#307 closed:** evolve Step 4 source_session bullet rewritten to require non-empty kebab-slug string with explicit `String(<object>)` warning + optional jq pre-write validation. NEW `tests/skills/vault-mirror-session-id-wiring.test.mjs` (45L, 3 tests) locks both wirings + the prompt contract. 4 waves coord-direct, ~15 min, 1868→1871 tests (+3).
- **2026-04-28 deep session — 25-issue agent-driven throughput:** 5 waves × 6 parallel agents under coordinator-direct orchestration in `main`, isolation:none enforcement:strict cap=6. Closed in single atomic commit `503e15a` (86 files / +9161 / -2921): #303 #304 (learnings/sessions JSONL writer Zod validation + migrate-* CLIs), #290 + #203 (bootstrap.lock plugin_version + MS_PER_DAY const), #289 (idempotent ecosystem-wizard), #178 (zombie-threshold-min wired end-to-end), #211 (SO_HOOK_PROFILE/SO_DISABLED_HOOKS profile gate), #283 #284 #285 (vault-mirror/config/categories splits — 679L→152L, 1075L→294L, 956L→17L barrel + 7 modules), #227 (pass/fail options-object with shim), #208 (ecosystem-wizard + worktree-freshness complexity), #228 (webhook-url.mjs centralize, personal-domain default removed), #308 (close auto-strip status:* labels), #269 (claude-md-drift command-count probe), #212 (CLAUDE_PLUGIN_ROOT 4-level fallback), #214 (express-path Phase 8.5 codified), #247 (vault-backfill YAML injection CWE-1336 yamlScalar fix), #156 (PSA-001 'aware' vs PSA-002 'pause' decision-tree), #266 (policy-cache effectiveness validation: KEEP, 100% hit-rate JSONL-on-disk), #264 (discovery-on-close session-type-aware default), #279 (schema-drift CI 403 → SCHEMA_DRIFT_TOKEN deploy-token + docs/ci-setup.md), #113 #112 (daily + vault-sync validator tests), #222 (harness-audit integration JSON truncation: maxBuffer=16MB + fixture pollution cleanup). Tests 1891 → 2160 (+269). Inter-wave Quality-Lite caught 3 minor lint regressions, fixed inline. GitLab origin push OK; GitHub mirror blocked by secret scanner false positive on `${SCHEMA_DRIFT_TOKEN}` placeholder URL pattern (manual unblock or amend of `.gitlab-ci.yml` auth pattern needed).
- **v3.2.0 GitHub release (2026-04-27 evening):** Consolidated stable cut covering 135 commits since v2.0.0. `package.json` 3.0.0-dev → 3.2.0, README badge sync, hooks/{hooks,hooks-codex}.json banner version sync (caught by `tests/hooks/banner-version-sync.test.mjs` on first quality run), `CHANGELOG.md` `[3.0.0] - Unreleased` + `[Unreleased] (dev trail)` blocks consolidated into a single `[3.2.0] - 2026-04-27` block (Added/Changed/Removed/Security/Quality/Migration sections covering v3.0+v3.1+v3.2 surfaces); old dev trail demoted to `## Internal Development Trail (pre-v3.2.0)` appendix. Public-voice `RELEASE_NOTES_v3.2.0.md` (4.3 KB) drafted (zero `gitlab.gotzendorfer.at`/`GH#`/`infrastructure/` refs), consumed by `gh release create v3.2.0 --latest --notes-file ...` then deleted. Tag pushed to both `github` (Kanevry/session-orchestrator) and `origin` (gitlab) — v3.0.0-rc.1 auto-demoted from Latest → Pre-release via `--latest` flag. 4 waves coord-direct, ~25 min, 1871 tests still green. Commit `e9a38bf chore(release): v3.2.0 — consolidated stable`.
- **2026-04-30 deep session — 4-cluster synergy (Owner Persona + DDD-Trio finish + script porting + stale close):** 5 waves × 6 parallel agents under coordinator-direct orchestration in `main`, isolation:none enforcement:strict cap=6. **Cluster A — Owner Persona (#161 epic)** shipped end-to-end: `scripts/lib/owner-yaml.mjs` (184L, 5 exports, 36 tests) + `scripts/lib/owner-interview.mjs` (130L, getInterviewQuestions/applyInterviewAnswers/runOwnerInterview) + `scripts/lib/soul-resolve.mjs` (98L, mustache-style `{{slot}}` resolver) + bootstrap SKILL.md Phase 3.5 (30 lines, owner interview wiring) + `.claude/rules/owner-persona.md` (79L, 7 sections, privacy guarantee) + bootstrap S99 wiring for owner-persona.md baseline-fetch + 15-test e2e flow. **Cluster B — Architecture-DDD-Trio (#309)**: Wave 1 discovery confirmed `skills/discovery/probes-arch.md` already shipped (235L, 4 probes incl. architectural-friction); only this `CLAUDE.md` narrative bullet remained. Cross-repo items #314/#315 deferred to projects-baseline MR. **Cluster C — Script porting (#218 partial)**: 6 top-level .sh ported to .mjs (codex-install, cursor-install, run-quality-gate, validate-wave-scope, validate-plugin, lib/fetch-baseline) with 5 .sh deletions (validate-wave-scope.sh was already absent pre-session). 12 docs/skill/rule files updated to point at .mjs paths. `scripts/lib/common.sh` KEPT per user decision (10 nested .sh in `scripts/lib/gates/` + `scripts/lib/validate/` still source it). Follow-up #317 created for nested ports + common.sh removal. **Cluster D — Stale tracking close**: #143 (vitest baseline functionally done — 2420 tests), #152/#153/#154 (all superseded by v3.2.0 shipped 2026-04-27) closed with evidence. #213 ComposioHQ submission draft at `docs/marketplace/composio-submission.md` (114L, comparison table + PR mechanics). #123 awesome-claude-code tracker comment updated. Inter-wave Quality-Lite caught 7 lint errors fixed inline by coordinator. Tests 2160 → 2420 (+260). Backlog 25 → ~17 open after closures.
- **2026-04-30 PM deep session — Cluster A finish (#317/#218) + Cluster B closures:** 5 waves × 6 parallel agents under coordinator-direct orchestration in `main`, isolation:none enforcement:strict cap=6. **Cluster A — script porting finish (#317 closes #218)**: 10 nested .sh files in `scripts/lib/{gates,validate}/` ported to .mjs siblings + `scripts/lib/common.sh` + `scripts/lib/platform.sh` deleted (`scripts/lib/` now zero `.sh`). New modules: `gate-{baseline,incremental,full,per-file,helpers}.mjs` (475L total) + `check-{plugin-json,component-paths,json-files,agents,commands}.mjs` (447L total). Orchestrators `scripts/run-quality-gate.mjs` + `scripts/validate-plugin.mjs` switched from `bash` → `node` spawn (5 + 5 path swaps). `.claude/rules/cli-design.md` "Shared Shell Library" section rewritten as "Shared Module Library (common.mjs)" reflecting the bash-free state. CONTRIBUTING.md updated (3 lines: `platform.sh` → `platform.mjs`, `common.sh` → `common.mjs`, env-var resolution note). **Cluster B — close-ready sweep**: #161 closed as D-axis-complete (D1-D4 shipped 2026-04-30 commit a992a5b; G-axis spawned as **#318** for cross-repo baseline propagation). #124 closed as superseded by v3.2.0 (2026-04-27, e9a38bf) — full Windows-native acceptance verified, last bash residual now eliminated. #305 kept open (`bleibt warn` — legacy data still 68%/89% skipped-invalid). #123 status keepalive comment posted. **Quality**: 165 new tests added (103 gates + 62 validate) + 5 orchestrator e2e tests in new `tests/scripts/orchestrators-e2e.test.mjs`. Tests 2420 → 2589 (+169). Inter-wave Quality-Lite caught 5 lint errors (3 W2 + 2 W3) fixed inline by coordinator. Final commit pushed to GitLab origin + GitHub mirror. Backlog 17 → 14 open after closures.
- **2026-04-30 main-2026-04-30-1635 deep session — 9-issue 5-cluster synergy bundle:** 5 waves × 6 parallel agents under coordinator-direct orchestration in `main`, isolation:none enforcement:strict cap=6. **Cluster C1 — Vault wiring (#319 ship):** New `scripts/lib/vault-staleness-banner.mjs` (142L, no-throw, 2-tier severity warn ≤48h / alert >48h) wired into `skills/session-start/SKILL.md` Phase 4 after bootstrap-lock-freshness; silent no-op when JSONL absent / malformed / stale_count=0. 17-test coverage (12 unit + 5 wiring snapshot). Issue closed. **Cluster C3 — Epic close-readiness:** #309 (Architecture-DDD-Trio adoption) closed — all plugin-scope items shipped (3 skills + probes-arch.md + 20 tests). #271 (v3.2 Autopilot epic) closed — all phases A/B/C-1/C-1.b/C-1.c/C-2/C-5 shipped, sub-issues #297/#298 correctly remain data-gated. **Cluster C4 — Autopilot data-gated scaffolds (#298 skeleton):** New `scripts/lib/evolve/autopilot-effectiveness.mjs` (~290L, type 8 = autopilot-effectiveness, schema_version:1 records). Data-gated on ≥20 paired manual+autopilot runs per mode; returns `[]` until threshold (safe early deploy). Dispatch wired into `skills/evolve/SKILL.md` (8th type entry + 6→8 counter sync, also fixed pre-existing missing #7 hardware-pattern in Step 3.5 enum). 15-test coverage. Issue stays open until first real learning emerges. **Cluster C5 — Baseline propagation prep:** Three preview drafts in `docs/baseline-diffs/2026-04-30-{owner-persona,architecture-rule,adr-template}-baseline.md` (#318, #314, #315 — 237 + 291 + 275 LOC). Cross-repo MR is a separate session — these are copy-paste-ready. **Cluster C2 — Marketplace:** #213 ComposioHQ submission verified PR-ready (0 TODOs, §6 fallback link valid, package.json/LICENSE/v3.2.0 tag confirmed). Stays open until external PR opens. #123 awesome-claude-code keepalive comment posted. **Cluster C5 — Watcher tracking (#305):** KEPT-OPEN with refreshed audit comment — skip-invalid 12%/15% (down from 87%/73% after #303/#304 fixes), threshold ≤5% per DoD; next step is migrate-CLI runs in housekeeping session. **Quality:** typecheck 54/54 OK, lint 0 errors, tests 2589 → **2623** (+34: 12 + 5 + 15 from W3 plus 2 incidental). **Doc-drift surfaced (non-blocking):** [RESOLVED 2026-05-08 in v3.4.0: plugin.json bumped to 3.4.0, skills/ confirmed 28 public entries, README + CLAUDE.md inventory lines synced.] Final commit pushed to GitLab origin + GitHub mirror. Backlog 14 → 12 open after closures (#309 + #271). <!-- consistency:exempt:alias-phrasing -->
- **2026-04-30 housekeeping — v3.3.0 release prep:** Cut after 11 commits since v3.2.0 (Owner Persona, AGENTS.md alias parity, bash-free milestone, vault-staleness banner, autopilot-effectiveness skeleton, refactor pass, 2 epic closures #309 #271). Bumped 5 metadata files: `package.json` 3.2.0→3.3.0, both `plugin.json` (`.claude-plugin/`, `.codex-plugin/`) 2.0.0→3.3.0 (catching up after 2 minor versions of drift), `hooks/{hooks,hooks-codex}.json` banner v3.2.0→v3.3.0. CHANGELOG `[Unreleased]` consolidated into `[3.3.0] - 2026-04-30` with structured Added/Changed/Fixed/Removed/Security/Closed/Quality/Migration sections covering all 11 commits (50+ closed sub-issues). Tests 2623 (12 skipped); typecheck 54/54 OK; lint clean. Commit + tag pushed to GitLab origin + GitHub mirror; `gh release create v3.3.0 --latest` auto-demoted v3.2.0 from Latest → historic.
- **2026-04-30 evening deep — Cluster D Baseline-Propagation MEGA-MR + Plugin-Housekeeping:** 5 waves × 6 parallel agents under coordinator-direct orchestration in `main`, isolation:none enforcement:strict cap=6. **Cross-repo MR (projects-baseline, branch `feat/baseline-propagation-2026-04-30`):** 5 propagation files closing 4 issues — `.claude/rules/owner-persona.md` (NEW 80L, byte-identical to plugin source, zero owner-data leakage; #318), `.claude/rules/architecture.md` (5 editorial patches: intro paragraph, raw-LANGUAGE-dump → 8-bullet vocabulary reformat fixing nested-H1 hierarchy bug, expanded skill-routing trigger phrases, new `Speculative seams` anti-pattern bullet, See Also strip 5→10 entries; 140L → 119L; #314), `docs/adr/000-template.md` (+17L `### What qualifies` 7 categories + `### What does not qualify` 4 anti-categories, sourced verbatim from `skills/domain-model/ADR-FORMAT.md`; Nygard structure preserved; ADRs 001-008 untouched; #315), `scripts/setup-project.sh` (+1 explicit cp line for owner-persona.md — pre-draft was wrong about glob-based copy), `templates/shared/CLAUDE.md.template` (+1 rule bullet). #240 closed as already-shipped (W2-A4 surprise: `scripts/tests/setup-project.bats` already exists with 267L, 15 @test, committed 356c4f9). **Plugin-side:** README.md (3 doc-drift patches: 25→26 skills with convergence-monitoring appended; 7→6 hook handlers; 2160→2623 tests) + CLAUDE.md inventory line synced + CHANGELOG `[Unreleased] ### Added` block (3 entries: convergence-monitoring #223, baseline propagation MR, plugin doc-drift sweep). **Branch hygiene:** initial branch creation accidentally based on `fix/ci-publish-gate-...-257-258` tip; recovered via soft-reset to main + git restore + targeted rm of foreign .changeset (foreign commits #257/#258 safely preserved on their own branch + origin remote). **Quality:** typecheck 54/54 OK, lint 0 errors, plugin tests 2623 → 2630 passed (no regressions), baseline bats 15/15 OK, markdown heading hierarchies validated. Inter-wave Quality-Lite ran cleanly after W2/W3/W4. Status comments posted on #305 (12% / 15.5% skip-invalid stable, watcher kept-open), #213 (ship-ready awaiting external PR), #123 (tracker keepalive). Final commits pushed to GitLab origin + GitHub mirror (plugin) + GitLab origin (projects-baseline MR opened). <!-- consistency:exempt:alias-phrasing -->
- **2026-05-01 deep session — #320 Express Path persistence + #238 archetype-gitkeep finish:** 5 waves × 6 parallel agents under coordinator-direct orchestration in `main`, isolation:none enforcement:warn cap=6. **Cluster A — #320 Express Path runtime fix:** `commands/go.md` 13 → 31 LoC (NEW Express Path Detection section: banner check → 5-step branch with `appendDeviation()` write before session-end invocation, post-close verification of `status: completed` + body contains `Express path:`); `skills/session-start/phase-8-5-express-path.md` +16 LoC (NEW Step 4 verification + Persistence Contract block correctly attributing the deviation write to `commands/go.md` not session-end + See Also); `scripts/lib/state-md.mjs` +103 LoC (2 NEW exports: `appendDeviation(contents, isoTimestamp, message)` + `markExpressPathComplete(contents, options)`, both no-throw with JSDoc); `commands/close.md` +1 cross-link in Rule 2 message hinting at Express Path audit trail. Test surface: `tests/skills/express-path-write-back.test.mjs` NEW (11 vitest cases — 7 spec assertions + 4 runtime assertions) + `tests/lib/state-md.test.mjs` 22 → 28 (+6 cases for new helpers). All 11 + 28 vitest tests green on first run. **Cluster B — #238 archetype-gitkeep finish (cross-repo projects-baseline):** 6 zero-byte `.gitkeep` files added across 3 archetypes added by `a9f0e0a` after the original Sub-Epic B shipment (`356c4f9`): `templates/{astro-content-site,go-service,tauri-desktop}/docs/{prd,retro}/.gitkeep`. `scripts/tests/setup-project.bats` 13 → 18 @test (3 archetype-presence cases). Bats 18/18 PASS, no regressions. **#231 + #239 already shipped on baseline `main` (commit 356c4f9)** — only closure comments needed (W5). **Quality:** typecheck 54/54 OK, lint 0 errors, full test suite stable (delta to be measured at W4 Full Gate). Inter-wave Quality-Lite green after W2/W3. Final commits pending W5: plugin commit on `main` + baseline new commit on `feat/baseline-propagation-2026-04-30` (PSA-003 disciplined staging — metrics drift unstaged).

For full release history, architectural decisions, and meta-audit narratives see [[01-projects/session-orchestrator/decisions]] in the Meta-Vault. The PRDs for v3.2 Autopilot live at [[01-projects/session-orchestrator/prd/2026-04-24-state-md-recommendations-contract|Phase A]] / [[01-projects/session-orchestrator/prd/2026-04-25-mode-selector|Phase B]] / [[01-projects/session-orchestrator/prd/2026-04-25-autopilot-loop|Phase C]].

## Session Config

persistence: true
enforcement: warn
recent-commits: 20
test-command: npm test
typecheck-command: npm run typecheck
lint-command: npm run lint
stale-branch-days: 7
plugin-freshness-days: 30
plan-baseline-path: ~/Projects/projects-baseline
plan-prd-location: docs/prd
plan-retro-location: docs/retro
plan-default-visibility: internal
vcs: gitlab
auto-skill-dispatch: false               # opt-in; phrase-match meta-skill — see skills/using-orchestrator/SKILL.md
vault-integration:
  enabled: true
  vault-dir: ~/Projects/vault
  mode: warn               # strict | warn | off
docs-orchestrator:
  enabled: false           # opt-in; when true, session-start Phase 2.5 runs + docs-writer agent available
  audiences: [user, dev, vault]
  mode: warn               # warn | strict | off
vault-staleness:
  enabled: false           # opt-in vault-drift probes (runs in /discovery vault)
  thresholds:
    top: 30                # days — tier=top narrative staleness threshold
    active: 60             # days — tier=active
    archived: 180          # days — tier=archived
  mode: warn               # warn | strict | off
persona-reviewers:
  enabled: false           # opt-in inter-wave architecture/QA/PRD audits
  reviewers: []            # ["architect-reviewer", "qa-strategist", "analyst"]
  mode: warn               # warn | strict | off
