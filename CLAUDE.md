# Session Orchestrator Plugin

> Project-instruction file resolution: this is `CLAUDE.md` on Claude Code / Cursor IDE; the equivalent file on Codex CLI is `AGENTS.md`. See [skills/_shared/instruction-file-resolution.md](skills/_shared/instruction-file-resolution.md).

> Für Installation, CLI-Nutzung und Architektur siehe [`README.md`](./README.md). Diese Datei enthält nur die runtime-kritischen Abschnitte, die von `skills/_shared/config-reading.md` gelesen werden.

## Structure

See [`README.md`](./README.md#components) for the canonical inventory (32 skills, 12 commands, 11 agents, 10 hook event matchers / 10 hook handlers). Runtime layout: `skills/`, `commands/`, `agents/`, `hooks/`, `.orchestrator/policy/`, `.claude/rules/`. Stable product/tech/structure context lives at `.orchestrator/steering/{product,tech,structure}.md` and is injected at session-start Phase 2.6 (when present).

## Destructive-Command Guard

`hooks/pre-bash-destructive-guard.mjs` blocks destructive shell commands in the main session (alongside subagent waves). Policy lives in `.orchestrator/policy/blocked-commands.json` (13 rules). Bypass per-session via Session Config:

```yaml
allow-destructive-ops: true
```

Rule source of truth: [`.claude/rules/parallel-sessions.md`](.claude/rules/parallel-sessions.md) (PSA-003). See README § Destructive-Command Guard for the full narrative.

## Agent Authoring Rules

Agent files live in `agents/` as Markdown with YAML frontmatter. Required fields:

```yaml
---
name: kebab-case-name                # 3-50 chars, lowercase + hyphens only
description: Use this agent when [conditions]. <example>Context: ... user: "..." assistant: "..." <commentary>Why this agent is appropriate</commentary></example>
model: inherit                        # inherit | sonnet | opus | haiku — OR full ID like claude-opus-4-7, claude-sonnet-4-6, claude-haiku-4-5-20251001
color: blue                           # blue | cyan | green | yellow | purple | orange | pink | red | magenta
tools: Read, Grep, Glob, Bash         # comma-separated string OR JSON array (both accepted; we prefer comma-string for consistency)
---
```

**Frontmatter spec source:** https://code.claude.com/docs/en/sub-agents § Supported frontmatter fields. Our local validator (`scripts/lib/validate/check-agents.mjs` + `scripts/lib/agent-frontmatter.mjs`) matches the canonical spec on `tools` (both forms accepted), `color` (canonical 8-color palette + magenta for backward-compat), and `model` (aliases + full IDs).

**Required vs optional:**
- Runtime canonical doc: only `name` + `description` are required.
- Our validator (defensive for plugin-distribution): all four of `name + description + model + color` required; `tools` optional.
- `description` MUST be a single-line inline string, NOT a YAML block scalar (`>` or `|`). Put `<example>` blocks inline.
- `tools` accepts BOTH comma-separated string (`Read, Edit, Write`) and JSON array (`["Read", "Edit", "Write"]`). Anthropic's own reference agents use array form; we use string form for consistency.

**Body conventions** (from Anthropic's `plugins/plugin-dev/agents/*` reference set):
- Sections: `**Your Core Responsibilities:**` → `**[X] Process:**` → `**Quality Standards:**` → `**Output Format:**` → `**Edge Cases:**`.
- Length: 500–3000 words is the recommended range. Below 500 reads as under-specified; above 3000 reads as bloated.
- Read-only reviewer agents: tools `Read, Grep, Glob, Bash` (no Edit/Write). Implementer agents: `Read, Edit, Write, Glob, Grep, Bash`.

Reference: https://github.com/anthropics/claude-code/blob/main/plugins/plugin-dev/skills/agent-development/SKILL.md

## Operational Rules <!-- consistency:exempt:runtime-only -->

- **CI status MUST be surfaced at session-start** — Local-only test runs are insufficient evidence of CI green. The 8-pipeline silent regression (2026-05-09 deep-3 → 2026-05-10 deep-1, fixed in deep-2) is the cautionary tale. Phase 4 of session-start invokes `scripts/lib/ci-status-banner.mjs` via `checkCiStatus({ repoRoot })` to render a 🚨 banner when CI is red on HEAD. Never assume CI is green from `npm test` results alone.

## Current State

- **Plugin version:** v3.5.0 (released 2026-05-09, GitHub + GitLab tag). Previous releases v3.4.0 (2026-05-08), v3.3.0 (2026-04-30), v3.2.0 (2026-04-27) at https://github.com/Kanevry/session-orchestrator/releases.
- **Active epic:** none — v3.2 Autopilot epic (#271) closed 2026-04-30 (all phases A/B/C-1/C-1.b/C-1.c/C-2/C-5 shipped). Sub-issues #297 (calibration, needs ≥10 RUNS) + #298 (evolve type 8, needs runtime data) remain data-gated on autopilot RUN-Volumen, not on code.
- **Stack:** Node 20+, vitest 4.1.5, ESLint 10. Run `npm ci` after cloning. Test: `npm test`. Lint: `npm run lint`. Coverage thresholds 70/65/70/60.
- **Tests:** 4966 passed / 12 skipped (post 2026-05-14 deep-3; +22 net from 4944 = +17 Q1 path-traversal+reporter regression + +5 Q-polish timeout_ms boundary). validate-plugin 36/36 (+2 from canary SCAN_ROOTS gaining `scripts/lib/shared/profiles/` after #393 relocation), typecheck 67/67, lint 0, doc-consistency 0 findings.
- **Backlog:** ~15-17 open issues post-deep-3. Closed this session: #385 (partial — mechanism proven, live-coverage gated on #396), #393, #390, #391, #394 (LOW-1/-3/-5 done; LOW-4 V2 deferred), #387 (in-repo done; baseline-dir deferred), #392. Filed this session: #395 (runner reporter regression-test gap), #396 (runner artifact shape vs rubric-v1), #397 (SEC-Q2 LOW cluster), #398 (runner runDir path-traversal — MED), #399 (runner AbortController test — MED), #400 (ARCH-MED-3 shared/ rename — MED), #401 (LOW cluster polish). Tracking-only: #305 · #213 · #123 · #341. Data-gated: #297 / #298. Low-pri chore: #372 schema_version v2 bump.
- **2026-05-14 deep-4 — /test pipeline housekeeping cluster (#396 #395 #398 #399 #400 #397 #401):** 5W × 6A coord-direct, commit `<pending>` (F6 backfill), ~22 sub-agents dispatched (6 W1 + 6 W2 + 6 W3 + 5 W4-reviewers + 1 W4-polish coord-direct + 1 W5 F1) + Q6 Full Gate coord-direct. Headline: **/test pipeline housekeeping cluster fully shipped end-to-end** — #396 artifact-shape gap CLOSED by reframing as **Division of Responsibility** (driver = infrastructure pre-creating `ax-snapshots/` + `screenshots/` dir scaffold; test fixtures = content emitting axe-*.json + console.ndjson; rubric-v1 + playwright-driver SKILL.md now carry an explicit DoR block — not a runner-code rewrite); #400 `shared/profiles` → `profiles/` rename landed with 6 mechanical refs + 2 `git mv` + 3 new R5 anchor canaries (validate-plugin **36 → 39**); #398 runDir path-traversal MED hardened with `isPathInside` guard at `scripts/lib/playwright-driver/runner.mjs:110`; #399 AbortController MED gap closed with 5 falsification-passing tests; #397 SEC-Q2 LOW symlink-resolution wrapped with two-phase realpath guard in `scripts/lib/config/test.mjs` + `scripts/lib/profiles/schema.mjs` (Phase 1 lexical `isPathInside`, Phase 2 realpath-when-exists, swallow ENOENT); #401 LOW polish cluster folded (`@lib/*` vitest alias + 2 test rewrites + peekaboo AUQ prose clarifier + glass-modifiers `RUBRIC_GLASS_V2=1` env-gate). W2 security-tightening fallout (5 `runner.test.mjs` `/tmp/run-001` rejections from #398 + 2 `schema.test.mjs` non-existent-path rejections from #397 strict realpath) all cleaned up in W3 P3/P4. Full Gate GREEN. **W1 Discovery (6 Explore, ~10m):** D1 (#396) audited runner.mjs artifact contract — **CRITICAL FINDING:** runner.mjs is correct; rubric-v1's `ax-snapshots/`, `axe-*.json`, `console.ndjson` namespaces are **test-fixture responsibility**, driver only pre-creates the directory scaffold and orchestrates spawn → fix is doc-sync + dir pre-creation, not artifact-writing logic; D2 (#398) traced runDir derivation at `runner.mjs:110` (`path.join(runsRoot, runId)` with no traversal check); D3 (#400) audited rename surface — 6 mechanical changes (5 imports + 1 SEED_PROFILES_PATH literal) + 2 `git mv` (`scripts/lib/shared/profiles/{registry,schema}.mjs` → `scripts/lib/profiles/{registry,schema}.mjs` + matching `tests/lib/shared/profiles/` → `tests/lib/profiles/`); D4 (#399) confirmed spawn + AbortSignal DI seam already exists in runner.mjs (no refactor needed — pure test-write); D5 (#395 + #397) confirmed deep-3's 8 reporter-syntax canaries already lock down `/html:/` + `/json:/` Jest-style substrings (#395 is a no-op), recipe for #397 = wrap `realpathSync` two-phase (lexical isPathInside → realpath escape check, reject on realpath failure when path exists); D6 (#401) enumerated exact line ranges — peekaboo SKILL.md AUQ prose at L142–148, glass-modifiers emit at `runner.mjs:198–204`, test-import depth at `tests/lib/profiles/{registry,schema}.test.mjs:1-3`. **W2 Impl-Core (6 file-disjoint code-implementer, parallel):** I1 (#396 + #398 merged): `scripts/lib/playwright-driver/runner.mjs` gained `import { isPathInside } from '../path-utils.mjs'` + path-traversal hard-error guard immediately after runDir derivation (rejects when `!isPathInside(runDir, runsRoot)`) + `mkdirSync({ recursive: true })` calls for `ax-snapshots/` and `screenshots/` sibling dirs pre-execution; I2 (#396 doc-sync): `skills/test-runner/rubric-v1.md` Check 1–4 rewrites + `skills/playwright-driver/SKILL.md` Artifact Layout section — both now carry an explicit **Division of Responsibility** block ("driver writes: scaffold, HTML+JSON reporter output, traces/. test fixture writes: ax-snapshots/, axe-*.json, console.ndjson"); I3 (#399): NEW `tests/lib/playwright-driver/runner-abort-controller.test.mjs` with 5 falsification-passing tests (timer-firing on timeout, AbortSignal propagation to child via spawn opts, clearTimeout on normal exit, exit-code 2 mapping on SIGTERM, custom `timeout_ms` from profile-registry override at 120000ms boundary); I4 (#397): `scripts/lib/config/test.mjs` `case 'profiles-path':` + `scripts/lib/profiles/schema.mjs` `rubric` field both wrapped with `realpathSync` two-phase guard (lexical `isPathInside` first, then realpath escape detection, reject on realpath failure ONLY when the resolved target exists outside boundary); I5 (#401-A): `vitest.config.mjs` gained `resolve.alias: { '@lib': new URL('./scripts/lib/', import.meta.url).pathname }` + rewrote 2 test files (`tests/lib/playwright-driver/runner.test.mjs` + `tests/lib/profiles/registry.test.mjs`) to use `'@lib/profiles/...'` imports; I6 (#401-B): `skills/peekaboo-driver/SKILL.md` Phase 3 AUQ prose clarifier (split multi-permission iteration paragraph) + `scripts/lib/playwright-driver/runner.mjs` glass-modifiers JSON emit gated behind `process.env.RUBRIC_GLASS_V2 === '1'` env var (default off — stays stub). **W2 fallout (cleaned in W3):** I1's #398 guard rejected 5 pre-existing tests in `runner.test.mjs` using literal `/tmp/run-001` paths (outside the configured runsRoot); I4's strict realpath made 2 `schema.test.mjs` tests fail because they pointed at non-existent fixture paths that the new guard rejects — plus revealed a `schema.mjs` import-depth off-by-one introduced by the I4 rename layout (fixed surgically in P4 deviation). **W3 Impl-Polish (6 agents, parallel):** P1 (#400 rename master + #401 GAP-Q4-5 anchor canaries): executed `git mv scripts/lib/shared/profiles → scripts/lib/profiles` + `tests/lib/shared/profiles → tests/lib/profiles` + 9 file edits (5 imports across runner.mjs/config/test.mjs/registry.mjs/registry.test.mjs/schema.test.mjs + 1 SEED_PROFILES_PATH literal + 3 doc-path refs) + 3 new R5 anchor canaries to `scripts/lib/validate/check-peekaboo-driver-canary.mjs` (`thin executor`, `$MISSING`, `status: stub`) — validate-plugin **36 → 39**; P2 (#400 docs sync): `skills/peekaboo-driver/SKILL.md` Composability Contract gained `(only emitted when RUBRIC_GLASS_V2=1)` caveat + `skills/test-runner/SKILL.md` + `skills/playwright-driver/SKILL.md` + `README.md` path-refs sweep verified zero `shared/` references in skills/ or README; P3: fixed 5 `runner.test.mjs` runDir-traversal failures by switching test fixtures from literal `/tmp/run-001` to `path.join(process.cwd(), '.orchestrator/metrics/test-runs', 'runner-test-...')` rooted within canonical runsRoot; P4: fixed 2 `schema.test.mjs` realpath failures by switching non-existent fixture paths to existing repo paths + added 3 GAP-Q4-4 boundary tests for profiles-path (empty-string, dot-resolved, absolute-inside) + surgical schema.mjs import-depth fix (`../../→../`) — deviation logged; P5 drafted W5 F1 narrative scratch at `.orchestrator/scratch/deep-4-narrative.md`; P6 read-only grep audit verifying zero remaining `scripts/lib/shared/profiles/` or `tests/lib/shared/profiles/` references in source/tests/docs. **W4 Quality (5 reviewers + 1 polish coord-direct + Q6 Full Gate coord-direct):** Q1 test-writer +9 gap-fill tests (3 #398 guard cases — `/etc/passwd`, `..` traversal, legitimate-pass; 2 mkdirSync canaries for `ax-snapshots/` + `screenshots/` pre-creation; 2 symlink-escape tests for config/test.mjs + profiles/schema.mjs; 2 `RUBRIC_GLASS_V2` gate-order canaries); Q2 security-reviewer **PROCEED-WITH-CAVEATS** — 0H / 1M / 1L + 3 nits (Q2-MED-1 runner.mjs runDir lexical-only — folded inline Q-polish via Phase 2 realpath upgrade; Q2-LOW-1 TOCTOU in `tcProfilesPath` storage of raw `v` before realpath — filed); Q3 architect-reviewer **PROCEED-WITH-CAVEATS** — 0H / 2M / 2L (Q3-MED-1 helper extraction `validatePathInsideProject` deferred to 3rd-callsite — filed; Q3-MED-2 `RUBRIC_GLASS_V2` should be profile-config flag not env-var, deferred to v2 rubric — filed; module-depth IMPROVED by #400 rename); Q4 qa-strategist **PROCEED** — 3 LOW gaps (PATH_MAX overflow, null-byte handling, double-abort idempotency — filed as LOW cluster); Q5 ux-evaluator **PROCEED-WITH-CAVEATS** — rubric Check 3 wording about console.log was misleading ("plain-text" residual phrasing) — folded inline Q-polish; **Q-polish coord-direct:** runner.mjs got Phase 2 realpath guard upgrade (folds Q2-MED-1 + Q3-MED-1 alignment) + rubric-v1.md L249 Check 3 wording fix (Q5); Q6 Full Gate **GREEN** — typecheck 67/67, lint 0, validate-plugin **39/39**, vitest **4982p/1f/12s** (1 fail = pre-existing worktree-freshness env flake, known/unrelated to scope), doc-consistency 0 findings. **W5 Finalization:** F1 (this) narrative + README counts verified · F2 issue-close batch (#396 #395 #398 #399 #400 #397 #401 + close #385 mechanism+coverage now aligned) · F3 evolve-prep harvest (3 learnings below) · F4 vault-mirror · F5 close-skill · F6 commit (no push per user). **Quality:** 4966p/12s → **4982p/1f/12s** (+16 net: +5 I3 abort-controller + +3 P4 boundary + +9 Q1 gap-fill − 1 P3-merged adjust; 1 fail is pre-existing env flake). validate-plugin **36 → 39** (+3 R5 anchor canaries). typecheck 67/67, lint 0, doc-consistency 0. **Filed follow-ups:** Q3-MED-1 (helper extraction `validatePathInsideProject`) · Q3-MED-2 (`RUBRIC_GLASS_V2` profile-config flag for v2 rubric) · Q4 LOW cluster (PATH_MAX overflow + null-byte handling + double-abort idempotency) · Q2-LOW-1 (TOCTOU `tcProfilesPath` raw-`v` storage) · worktree-freshness env-flake observation [issue numbers backfilled by F2]. **Carryover:** #385 (mechanism + rubric-v1 coherence now DONE this session — close at F2) · #386 (mac-gate end2end — defer, bootstrap-cost trap) · #387 (projects-baseline absent — defer) · #394 LOW-4 (peekaboo runner.mjs V2 — defer to test-runner v2). **Learnings to /evolve:** (a) **Division-of-Responsibility** pattern (driver pre-creates dir scaffold + emits infrastructure artifacts like HTML/JSON/traces; test fixture emits content artifacts like axe-*.json + console.ndjson) is the canonical shape for any test driver-skill pair — apply when rubric-vs-driver artifact-shape disputes arise; (b) **two-phase realpath guard** (Phase 1 lexical `isPathInside`, Phase 2 symlink-resolution-when-exists with ENOENT swallow) is the canonical shape — applied 3x this session (`config/test.mjs`, `profiles/schema.mjs`, `runner.mjs` Q-polish); (c) when a W2 source rename happens (#393 deep-3 had it; I4 deep-4 didn't, but P4 surfaced an import-depth off-by-one), test-suite import-error reveals the issue immediately — a **`@lib/*` vitest config alias** prevents this entire class of churn during file relocations and should be applied repo-wide.

- **2026-05-14 deep-3 — #385 live-run + housekeeping cluster (#393 #390 #391 #394 #387 #392):** 5W × ≤6A coord-direct, commit `<pending>`, ~18 sub-agents dispatched (6 W1 + 2 W2 + 3 W3 + 4 W4 + 1 Q-polish + 1 W5 F1) + 3 coord-direct phases (W1 /test live-run + W4 Full Gate + W5 commit). Headline: **/test value-proof mechanism live-verified end-to-end** against EspoCRM-on-aiat-pmo-module — bootstrap (npm install + chromium binary) succeeded, runner.mjs spawned Playwright, real artifacts (HTML+JSON+traces) written, classified 1 unexpected fail + 31 skipped. Two mid-wave hotfixes surfaced bugs the deep-2 dry-run mechanism-proof missed: **(a) runner.mjs reporter syntax bug** (`--reporter html:<path>,json:<path>` Jest/Vitest-style; Playwright rejected with "Cannot find module 'html:<path>'") — fixed inline coord-direct via canonical `--reporter html,json` + env vars `PLAYWRIGHT_HTML_OUTPUT_DIR`/`PLAYWRIGHT_JSON_OUTPUT_FILE`/`PLAYWRIGHT_HTML_OPEN=never`, grounded in playwright.dev/docs/test-reporters via ref-mcp + WebFetch; **(b) target-resolution gap** — nested `tests/e2e/package.json` requires `--target <repo>/tests/e2e` not `<repo>` root (else `npx playwright` falls back to global, conflicts with project-local @playwright/test). Full Gate GREEN. **W1 Discovery (6 Explore, ~7m):** D1 audited profile-{registry,schema}.mjs callers (only 1 cross-layer: playwright-driver/runner.mjs:32; 27 tests; recommended target `scripts/lib/shared/profiles/`); D2 extracted verbatim peekaboo↔playwright wording diffs (LOW-1 + LOW-3 scope) + AUQ multi-item patterns from discovery skill (Pattern A); D3 mapped #390/#391 insertion points (`config/test.mjs:73-74` + `profile-schema.mjs:90-94`) + identified canonical `isPathInside(child, parent)` from `scripts/lib/path-utils.mjs` (CWE_23_ATTACK_PATTERNS); D4 confirmed `~/Projects/projects-baseline/` ABSENT → #387 scope-reduced to project-instruction-file (`CLAUDE.md` or `AGENTS.md`) + README count edits (D4 also confirmed validate-plugin already covers all 5 new /test files via existing checks — no new check entries needed); D5 confirmed glass-modifiers JSON already has `schema_version: v1` (LOW-5 fix = add `status: stub` + rubric-v1.md note) + recommended `.claude/rules/testing.md` § "CI Integration" as #392 target; D6 confirmed aiat-pmo-module live-stack 4/4 containers UP (3h uptime), baseURL http://localhost:8090 → 200, 15 .spec.ts files, `runner.mjs` exit codes 0/1/2 mapped. **W1 coord-direct (#385):** bootstrap completed in ~30s (4 npm packages + chromium-headless-shell 92.4 MiB to `~/Library/Caches/ms-playwright/chromium_headless_shell-1223`); 3 live-run attempts surfaced runner.mjs reporter syntax bug → fixed inline (deviation logged) → target gap → 3rd run produced 31 skip + 1 fail (initiative-list.spec.ts:27 401 auth gap, missing skip-guard — cross-repo finding). Proof report `docs/test-runs/proof-aiat-pmo-module-2026-05-14.md`; mechanism FULLY proven, coverage-proof PARTIAL (rubric-v1 expects `ax-snapshots/`, `axe-*.json`, `console.ndjson` namespaces runner doesn't yet write → filed #396 as V2 gating issue). **W2 Impl-Core (2 parallel file-disjoint):** I1 (#393) `git mv` relocation `scripts/lib/test-runner/profile-{registry,schema}.mjs` → `scripts/lib/shared/profiles/{registry,schema}.mjs` + tests + 1 cross-layer import in `playwright-driver/runner.mjs:32` updated (`../test-runner/profile-registry.mjs` → `../shared/profiles/registry.mjs`); I2 (#387 partial) line 9 of `CLAUDE.md` (or `AGENTS.md` on Codex CLI) + `README.md` Components counts 31→32 skills, 11→12 commands; baseline sub-item deferred (dir absent). **W3 Impl-Polish (3 agents, P3-P5 merged):** P1 (#390) added `isPathInside`-backed traversal validation in `scripts/lib/config/test.mjs` `case 'profiles-path':` — silent-skip on rejection (matches case 'mode' pattern); P2 (#391) added same validation in relocated `scripts/lib/shared/profiles/schema.mjs` `rubric` field — default `'skills/test-runner/rubric-v1.md'` still validates (critical regression guard); P3-merged (#394 LOW-1+LOW-3+LOW-5) peekaboo-driver SKILL.md soul-onboarding parity wording + Phase 3 AUQ multi-permission template iterating over `$MISSING` + glass-modifiers JSON `status: stub` field + rubric-v1.md Check 4 note. **Mid-W3 coord-direct fix:** W2 I1's test files used 3-level `../../../` imports but new path `tests/lib/shared/profiles/` is one level deeper → patched to `../../../../` (4 levels) in 3 places (2 imports + 1 SEED_PROFILES_PATH literal); 30 tests now pass on relocated modules. **W4 Quality (4 reviewers + Q-polish + coord-direct Full Gate):** Q1 test-writer +17 net tests (4 profiles-path traversal + 5 rubric traversal + 8 reporter-syntax regression canaries with `/html:/` and `/json:/` substring-rejection lockdowns); Q2 security-reviewer PROCEED-WITH-CAVEATS — 0H/0M/2L (SEC-Q2-LOW-1 symlink-resolution gap, SEC-Q2-LOW-2 SEC-PD-MED-2 guard test coverage); Q3 architect-reviewer PROCEED-WITH-CAVEATS — 0H/3M/3L (ARCH-Q3-MED-1 canary SCAN_ROOTS stale, MED-2 playwright SKILL.md reporter doc-drift, MED-3 `shared/` junk-drawer name; LOW-1 test-import depth, LOW-2 AUQ prose, LOW-3 glass-modifiers flag-gate) — **#393 ARCH-PD-MED-3 CLOSED**; Q4 qa-strategist 5 gaps (Q4-1 runner runDir traversal MED, Q4-2 timeout_ms boundary MED, Q4-3 AbortController test MED, Q4-4 profiles-path boundaries LOW, Q4-5 SKILL.md polish canaries LOW); Q-polish folded ARCH-MED-1 (canary SCAN_ROOTS +`scripts/lib/shared/profiles/` in both `check-{playwright-mcp,peekaboo-driver}-canary.mjs` → validate-plugin 34→36) + ARCH-MED-2 (playwright SKILL.md reporter doc-drift, removed all `html:<path>` references with canonical-docs reference) + GAP-Q4-2 (+5 timeout_ms boundary tests: 0, -1, 120000.5, 3_600_001, 3_600_000-at-ceiling); Full Gate GREEN — typecheck 67/67, lint 0, validate-plugin 36/36, vitest 4966p/12s, doc-consistency 0 findings. **W5 Finalization:** F1 docs-writer extended `.claude/rules/testing.md` § "CI Integration" with `### Shared-Hardware Runner Contention (Mac shell executors)` subsection (~32 lines, cites pipeline #3940 evidence + 3 ordered mitigations); F2 (this) narrative + closes + invoke session-end. **Quality:** 4944p/12s → 4966p/12s (+22 net); validate-plugin 34→36 (+2 expanded canary roots). **Filed follow-ups:** #395 runner reporter regression-test gap (MED) · #396 runner artifact-shape vs rubric-v1 (high) · #397 SEC-Q2 LOW cluster · #398 runner runDir traversal MED · #399 runner AbortController test MED · #400 ARCH-MED-3 shared/ rename MED · #401 LOW cluster polish. **Carryover:** #385 live-coverage-proof (mechanism done; full rubric-v1 coverage gated on #396) · #386 MAC-PROOF end2end · #394-LOW-4 (peekaboo runner.mjs V2) · #387 projects-baseline-dir (external repo absent). **Learnings to /evolve:** (a) playwright reporter canonical syntax (use `--reporter html,json` + env vars; never `html:<path>` Jest-style); (b) dry-run mechanism-proof misses spawn-level bugs — live execution is required for value-proof closure; (c) nested-package targets need profile schema enhancement (currently work-around: pass `--target <repo>/tests/e2e` directly).

- **2026-05-14 deep-2 — /test Track B (#381) + #385 mechanism-proof:** 5W coord-direct, commit `<pending>`, 14 agents dispatched (5 W1 + 5 W2 + 1 W3 + 3 W4-reviewers + 1 W4-polish + 1 W5 F1). Headline: peekaboo-driver skill + `playwright-driver/runner.mjs` unblocked the end-to-end /test pipeline; `runner.mjs` was identified as a critical missing piece (D5) and delivered as I5; EspoCRM live coverage-proof deferred (carryover #385). Full Gate GREEN. **W1 Discovery (5 Explore, ~15m):** D1 peekaboo canonical package confirmed `@steipete/peekaboo` v3.1.2; full CLI surface mapped (8 sub-commands: permissions/see/click/type/scroll/image/run/agent); D2 macOS permissions JSON shape verified live — 2 hard (Screen Recording, Accessibility) + 1 optional (Event Synthesizing); D3 structural template extracted from `skills/playwright-driver` — 7 divergences identified (no headless-mode, no multi-browser, +Permissions Setup phase, etc.); D4 aiat-pmo-module confirmed GO — 15 Playwright tests in `tests/e2e/`, EspoCRM compose at `dev/docker-compose.yml`; later discovered nested `package.json` + heavier bootstrap than anticipated; D5 **critical blocker** — `scripts/lib/playwright-driver/runner.mjs` missing; /test cannot run end-to-end without it. **W1.5 plan adjustment (user-approved AUQ):** W2 expanded 4 → 5 agents (added I5 runner.mjs ~80 LOC estimated); axe-core soft-skip protocol researched (`require.resolve` / `package.json` detection); both decisions grounded in playwright.dev CLI docs + Node `spawn`/`AbortSignal` patterns. **W2 Impl-Core (5 file-disjoint code-implementer):** I1 `skills/peekaboo-driver/SKILL.md` (216 LOC, 3-phase platform + permission + remediation gate, AX-snapshot pattern, glass-modifiers v1 stub); I2 `skills/peekaboo-driver/soul.md` (38 LOC); I3 `scripts/lib/validate/check-peekaboo-driver-canary.mjs` (R5 grep-canary, +3 PASS sub-checks → validate-plugin 31 → 34); I4 `skills/test-runner/SKILL.md` surgical edits (4 changes: lines 10/50/94-102/288, peekaboo placeholder → real dispatch contract); I5 `scripts/lib/playwright-driver/runner.mjs` (260 LOC total, ~140 executable; `spawn` + `AbortSignal`, profile registry integration, axe-core soft-skip, exit-code mapping 0/1/2, DI seams `spawn`/`fs`). **W3 Impl-Polish + mechanism-proof:** P1 `docs/test-runs/` scaffold + `_template-proof-report.md`; coord-direct mechanism-proof via `node scripts/lib/playwright-driver/runner.mjs --dry-run` against `~/Projects/intern/aiat-pmo-module/tests/e2e` → 5 mechanism guarantees verified (CLI args, tilde expansion, profile load, axe soft-skip, command resolution); scope-reduction approved — EspoCRM live stack bootstrap (prod-DB dump, dev/.env secrets, `tests/e2e` npm install, playwright install chromium, docker compose up) too heavy for in-session work → #385 partial-closed at mechanism-proof; live coverage-proof carries over; proof artifact `docs/test-runs/proof-mechanism-2026-05-14.md`. **W4 Quality (3 reviewers + 1 polish + coord-direct Full Gate):** Q1 test-writer +47 net tests (peekaboo-driver-frontmatter 20 + canary 11 + runner 17); Q2 security-reviewer PROCEED-WITH-CAVEATS — 0 HIGH / 2 MED / 3 LOW (SEC-PD-MED-1 shell injection via TARGET, SEC-PD-MED-2 reporter arg-injection via runDir, SEC-PD-LOW-1 path-traversal, SEC-PD-LOW-2 canary-exempt bypass marker, SEC-PD-LOW-3 timeout_ms V8-overflow); Q3 architect-reviewer PROCEED-WITH-CAVEATS — 0 HIGH / 3 MED / 5 LOW (ARCH-PD-MED-1 speculative `opts.now` seam, ARCH-PD-MED-2 `writeFileSync` doc-drift, ARCH-PD-MED-3 cross-layer import, 5× cross-skill asymmetry LOWs); Q-polish folded 4 MED + 2 LOW inline (SEC-MED-1/2, ARCH-MED-1/2, SEC-LOW-2/3); ARCH-MED-3 + 5 remaining LOWs filed as follow-up issues; Q4 Full Gate GREEN — typecheck 67/67, lint 0, validate-plugin 34/34, vitest 4944p/12s, doc-consistency 0. **Quality:** 4897p/11s → 4944p/12s (+47 net, +1 skipped). validate-plugin 31/31 → 34/34 (+3 R5 peekaboo canary rules). **Filed follow-ups:** TBD by F2 close. **Carryover:** #385 EspoCRM live coverage-proof · #386 MAC-PROOF end2end · #387 projects-baseline adoption. Commit `<pending>` (F2 backfill).
- **2026-05-14 deep-1 — CI restore + #383 /test command + #384 issue-reconcile glab wiring (#383 #384 #388 #389):** 5W coord-direct ~1h30m total wall-clock, 17 agents dispatched (5 W1 + 6 W2 + 2 W3 + 4 W4 + 1 W5 F1), 2 coord-direct phases (Q3 Full Gate, F2 commit-push-CI). Headline: CI was red on HEAD `cb3e942` (both GitLab + GitHub) going into this session; all 4 scope items shipped + 2 security findings folded inline. **W1 Discovery (5 Explore, ~7m):** D1 root-cause: `skills/vault-sync/pnpm-lock.yaml` tracked despite `.gitignore:63` forbidding it — lockfile conflict + `engine-strict=true` → silent npm install exit-1 (confidence 0.95); D2 confirmed `fetch-depth: 1` on `.github/workflows/test.yml:23` breaks gitleaks revision-range; D3 /test command shape + argument precedence + handoff contract; D4 Session Config `test:` block schema + profile registry path; D5 #384 signatures + #388/#389 fold-in YES recommendation. **W2 Impl-Core (6 file-disjoint code-implementer, parallel):** I1 `.gitlab-ci.yml` dropped `--silent` + `git rm --cached skills/vault-sync/pnpm-lock.yaml`; I2 `.github/workflows/test.yml:23` `fetch-depth: 1 → 0`; I3 NEW `commands/test.md` (86 LOC, frontmatter + 6 sections + AUQ + handoff contract); I4 PARTIAL — `scripts/parse-config.mjs` parser + `docs/session-config-reference.md` done; `scripts/lib/config.mjs` parity deferred to W3 P1; I5 NEW `scripts/lib/test-runner/profile-{schema,registry}.mjs` + `.orchestrator/policy/test-profiles.json` (web-gate + mac-gate seeds); I6 extended `issue-reconcile.mjs` with `listExistingFindings/createFinding/updateFinding/triageDecision` + #388 + #389 hardening, 35 existing tests preserved. **W3 Impl-Polish (2 parallel):** P1 extracted `_parseTest` to `scripts/lib/config/test.mjs`; wired into `scripts/lib/config.mjs`; parity test now passes; P2 appended `pnpm-lock.yaml` to `skills/vault-sync/.gitignore` (preventive regression guard). **W4 Quality (4 agents):** Q1 test-writer +60 net tests (profile-schema 10 + profile-registry 20 + config/test 13 + issue-reconcile +16 extension); Q2 security-reviewer PROCEED-WITH-CAVEATS — 1 HIGH (glabPath arbitrary-binary injection) + 2 MED (reconcileFinding body-length cap, sanitizer case-sensitivity) + 2 LOW (profiles-path + rubric path-traversal); Q-polish code-implementer folded HIGH + 2 MED inline — removed `glabPath` param, added `opts.execFile` DI seam mirroring `mr-draft.mjs`, added `checkBodyLength`, sanitizer regex `→ gi` flag, +11 regression tests; Q3 Full Gate **GREEN** — typecheck 67/67, lint 0, validate-plugin 31/31, doc-consistency 0, tests 4897p/11s. **Notable learning:** Plugin agents require `session-orchestrator:` namespace prefix in `Agent({ subagent_type })` calls — Wave-loop.md § 1 doesn't state this explicitly; initial W2 dispatch lost 6 agents to "agent type not found" before re-dispatch with prefix. Saved as anti-pattern learning `agent-dispatch-plugin-namespace-prefix-required` (confidence 0.95) in `.orchestrator/metrics/learnings.jsonl`. **Quality:** 4826p/12s → 4897p/11s (+71 net, −1 skipped). typecheck 67/67, lint 0, validate-plugin 31/31, doc-consistency 0. **Filed follow-ups:** #390 (SEC-IR-LOW-2 profiles-path path-traversal validation) · #391 (SEC-IR-LOW-3 rubric path-traversal validation). **Carryover:** #381 peekaboo-driver Track B · #385 WEB-PROOF end2end · #386 MAC-PROOF end2end · #387 projects-baseline adoption. Commit on main; CI post-push (SHA backfilled in next session narrative).
- **2026-05-14 deep-1 — Track A of /test epic (#378): ux-evaluator + playwright-driver + test-runner skeleton (#379 #380 #382):** 5W coord-direct ~25min total agent time, isolation:none per #243 (new-dir detection), 14 agents dispatched. Headline: first 3 issues of the /test dependency chain land — agent, driver skill, and test-runner helper modules. **W1 Discovery (5 Explore, ~7m):** D1 skill/phase-model audit confirmed test-runner fits as a new skill (not a command extension); D2 agent frontmatter audit verified `blue`/`opus` validator-compatible for `ux-evaluator` (palette now at 11 agents on 9 colors — `blue` collides with `architect-reviewer` and `cyan` already collides between `docs-writer`/`qa-strategist`, both tolerated since neither pair is co-dispatched in practice; palette expansion is a known follow-up); D3 playwright probe discovered the PRD carried a wrong package name (`@playwright/cli@0.1.13` is an unrelated stub — canonical is `playwright@1.60.0` with binary `playwright`), corrected inline in skill before any code landed; D4 validator-extension audit confirmed `check-playwright-mcp-canary.mjs` pattern and wiring point in `validate-plugin.mjs`; D5 helper-module spec resolved export signatures for `fingerprint.mjs`, `artifact-paths.mjs`, `issue-reconcile.mjs`. **W2 Impl-Core (4 code-implementer, isolation:none):** I1 (#379) `agents/ux-evaluator.md` (~141 LOC) + `skills/test-runner/rubric-v1.md` (~373 LOC) — 4-check rubric (onboarding step-count ≤7, axe-violations critical/serious, console-errors visible to user, Apple-Liquid-Glass `.glassEffect()` conformance on SwiftUI 26+); I2 (#380) `skills/playwright-driver/SKILL.md` (~201 LOC) + `skills/playwright-driver/soul.md` (~30 LOC) — MCP-wired driver with artifact layout (`screenshots/`, `traces/`, `axe-*.json`, `console.ndjson`); I3 (#382-A) `skills/test-runner/SKILL.md` (~279 LOC) + `skills/test-runner/soul.md` (~46 LOC) — phase model (Setup → Drive → Evaluate → Reconcile → Report), Phase 2 peekaboo-driver placeholder graceful-absent; I4 (#382-B) `scripts/lib/test-runner/fingerprint.mjs` + `artifact-paths.mjs` + `issue-reconcile.mjs` + `scripts/lib/validate/check-playwright-mcp-canary.mjs` — pure helper modules, no side effects, DI-friendly. **W3 Impl-Polish (2 code-implementer, ~3.5m):** P1 wired R5 grep-canary into `validate-plugin.mjs` (check count 28 → 31, +3 new R5 rules); P2 cross-skill integration audit found and fixed 2 defects — (a) playwright-driver was documenting `.yaml` AX dumps but rubric Check 2 needs `axe-*.json`, reconciled in driver layout + rubric; (b) rubric described console.log as "plain-text prefixed by log level" but driver writes NDJSON `{ts,type,text,location}`, reconciled to NDJSON in rubric + evaluator parser step. **W4 Quality (3 agents + coord-direct Full Gate, ~6m):** Q1 test-writer 69 tests across 3 helper modules (`fingerprint.test.mjs` 16 tests, `artifact-paths.test.mjs` 18 tests, `issue-reconcile.test.mjs` 35 tests) — covers happy paths, edge cases, invalid inputs, normalisation invariants; Q2 ux-evaluator frontmatter regression 17 tests (`ux-evaluator-frontmatter.test.mjs`) — name/description/model/color/tools validation + floor/ceiling agent-count canary; Q3 security-reviewer PROCEED-WITH-CAVEATS — SEC-IR-MED-1: `recommendation` field in `issue-reconcile.mjs` lacks sentinel-injection hardening when consumed by glab (filed #388); SEC-IR-LOW-1: `maxBuffer` + body-length cap parity with `mr-draft.mjs` not yet enforced (filed #389); both non-blocking; Q4 Full Gate GREEN — typecheck 67/67, lint 0, validate-plugin 31/31, doc-consistency 0, tests 4826p/12s. **W5 Finalization (this wave):** CLAUDE.md (or AGENTS.md on Codex CLI) narrative + README component count bump + #388 + #389 follow-up issues confirmed filed. **Notable corrections:** (1) PRD package-name: `npm i -g @playwright/cli@latest` was wrong — `@playwright/cli@0.1.13` is an unrelated low-version package; canonical is `playwright@1.60.0`; documented inline in `skills/playwright-driver/SKILL.md` to prevent re-introduction; (2) cross-skill artifact format mismatch: driver documented `.yaml` AX dumps, rubric + evaluator expected `axe-*.json` NDJSON — both files reconciled to NDJSON in W3. **Quality:** 4740p/12s → 4826p/12s (+86 net, 0 skipped delta). typecheck 67/67, lint 0, validate-plugin 28/28 → 31/31 (+3 R5 canary rules), doc-consistency 0. **Filed follow-ups:** #388 (SEC-IR-MED-1 sentinel-injection hardening) · #389 (SEC-IR-LOW-1 maxBuffer/body-length cap). **Carryover:** #381 peekaboo-driver Track B · #383 `/test` command file + Session Config field + profile registry schema · #384 issue-reconciliation glab wiring + triage AUQ · #385 WEB-PROOF end2end · #386 MAC-PROOF end2end · #387 projects-baseline adoption. Commit on main; pipeline TBD post-push.

For older session narratives (2026-05-08 → 2026-05-12), release histories, and meta-audit fallout see [[01-projects/session-orchestrator/decisions]] in the Meta-Vault. Quick index of most recent commits: `<pending>` (2026-05-14 deep-4 — /test pipeline housekeeping cluster) · `<pending>` (2026-05-14 deep-3 — #385 live-run + cluster) · `253a4ab` (2026-05-14 deep-2 — peekaboo-driver + runner.mjs) · `3aee4cc` (2026-05-14 deep-1 — /test command + reconcile glab) · `cb3e942` (2026-05-14 Track A) · `a5c354e` (2026-05-12 #214 stub) · `5cfa469` (2026-05-12 #378 PRD) · `7b71573` (2026-05-12 #375/376/377) · `abd82aa` (2026-05-10 #374) · `eb820ca` (2026-05-11 #370/371/373) · `12c0df4` (2026-05-10 #364-substrate) · `ed83019` (2026-05-10 CI restoration) · `7158b82` (2026-05-08 v3.4.0). The PRDs for v3.2 Autopilot live at [[01-projects/session-orchestrator/prd/2026-04-24-state-md-recommendations-contract|Phase A]] / [[01-projects/session-orchestrator/prd/2026-04-25-mode-selector|Phase B]] / [[01-projects/session-orchestrator/prd/2026-04-25-autopilot-loop|Phase C]].

## Session Config

persistence: true
enforcement: warn
recent-commits: 20
test-command: npm test
typecheck-command: npm run typecheck
lint-command: npm run lint
stale-branch-days: 7
plugin-freshness-days: 30
plan-baseline-path: ~/Projects/projects-baseline
plan-prd-location: docs/prd
plan-retro-location: docs/retro
plan-default-visibility: internal
vcs: gitlab
auto-skill-dispatch: false               # opt-in; phrase-match meta-skill — see skills/using-orchestrator/SKILL.md
vault-integration:
  enabled: true
  vault-dir: ~/Projects/vault
  mode: warn               # strict | warn | off
docs-orchestrator:
  enabled: false           # opt-in; when true, session-start Phase 2.5 runs + docs-writer agent available
  audiences: [user, dev, vault]
  mode: warn               # warn | strict | off
vault-staleness:
  enabled: false           # opt-in vault-drift probes (runs in /discovery vault)
  thresholds:
    top: 30                # days — tier=top narrative staleness threshold
    active: 60             # days — tier=active
    archived: 180          # days — tier=archived
  mode: warn               # warn | strict | off
persona-reviewers:
  enabled: false           # opt-in inter-wave architecture/QA/PRD audits
  reviewers: []            # ["architect-reviewer", "qa-strategist", "analyst"]
  mode: warn               # warn | strict | off
