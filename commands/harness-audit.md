---
description: Run the harness audit and report the rubric score for this repository
argument-hint: ""
---

# Harness Audit

The user wants to audit this repository against the session-orchestrator harness rubric. There are no arguments.

**Invoke the `harness-audit` skill** (`skills/harness-audit/SKILL.md`). It runs `node scripts/harness-audit.mjs` and defines exactly what to surface from its stderr summary and stdout JSON record.
